---
title: 応用情報技術者試験 引き継ぎ
version: 3.0.0
created: 2026-08-02
updated: 2026-08-03
対象: このフォルダ（応用情報技術者試験）全体
---

# 応用情報技術者試験フォルダ 引き継ぎドキュメント

> **このファイルの役割**
> このフォルダの作業を、セッションをまたいで・モデルをまたいで同じ品質で継続するための
> **唯一の基準書**。**作業を始める前に必ず全文を読むこと。**
> 方針・書式・分担・進捗はすべてここで管理する。

> # 🎯 このプロジェクトの目的
> ## **試験範囲を網羅し、応用情報技術者試験に合格すること。**
>
> **教科書がその主目的そのもの**であり、範囲の網羅は `応用情報技術者試験_出題範囲詳細.md`（IPA出題範囲）で判定する。
> **用語集と単語リストは、教科書を支えるための補助**である。主従を取り違えないこと。
> 補助を基準にして教科書を組み立てると、**出題範囲に穴が空いたまま「完成」と誤認する**
> （2026-08-03 に実際に発生。→ 2.5章冒頭）。

---

# 🚀 セッション開始時の3分ブリーフィング

**セッションをクリアした直後でも、ここだけ読めば前回と同じ理解度で再開できる。**

## 現在地（2026-08-03 時点）

| 成果物 | 進捗 | 次の一手 |
|---|---|---|
| **教科書**（メイン・全23分野） | **2/23** 完成（11_セキュリティ／**10_ネットワーク**） | **ネットワークの用語集F1（設計）** ← これが次【Opus】 |
| **用語集**（拡張・分野ごと） | **140語** 完成（11_セキュリティのみ） | **ネットワーク74語**に着手できる状態（教科書が完成したため） |
| **F5 リンク設定**（教科書→用語集） | ✅完了（セキュリティ・189本） | ネットワークのF5は用語集F3の完了後【Sonnet】 |
| 公開サイト | 稼働中・全ページ到達確認済み | ─ |

> **ネットワーク教科書（2026-08-03 完成）の要点**
> **初版は単語リストを骨格に組み立てたため、IPA出題範囲にある項目が抜けていた**（ユーザー指摘で発覚）。
> 出題範囲詳細と突合して1.1.0で補完済み。
> 現在は**中分類10の小分類1〜5の知識項目例をすべて本文で説明している**（＝完了条件を満たしている）。
> **次の分野を書く担当は、2.5章冒頭の「教科書の工程では単語リストを開かない」を必ず守ること。**
> 全節に番号を振ってあるため、用語集からは `10_ネットワーク` の「N.M 節」で参照できる（罠#5を回避済み）。
> 分量は約57,300字（1.1.0時点）で、2.5章の目安（20,000〜30,000字）を上回っている（→ 2.5章に実績と理由を記録）。

> ✅ セキュリティは相互リンクが完成済み（教科書→用語集189本／用語集→教科書140本）。
> ただし**189本はホバー実測で罠を踏んで修正した後の数**（当初202本→バグ発覚→189本に修正）。
> 次分野で同じスクリプトを使う際は、**必ず2.6章の「実ファイル名を主キーにする」実装を使うこと**（罠#8）。

### 📌 未着手TODO（ユーザーの指示待ち・勝手に着手しない）

| # | 内容 | 状態 | 詳細 |
|---|---|---|---|
| **TODO-1** | 高解像度モニターで本文が広がらない（3440pxでも本文930px＝画面の27%） | **2026-08-03 実装・CDP検証済み・push＆デプロイ済み**。問題なし | → 2.8章 |
| **TODO-2** | 用語クリックでサイドパネルに用語ページを表示 | **✅2026-08-03 完了**。自作で実装しデプロイ済み。**本番環境でCDP全41項目合格** | → **2.9章**（2.8章は失敗記録） |

> 2026-08-02 の調査時点の原因分析は**誤りだった**（グリッドの1500px頭打ちが原因、という推測）。
> 2026-08-03 にCDPで実測したところ、真因は `@quartz-themes/core` が注入する
> `div#quartz-root.page`（id込みの詳細度）の `max-width:100dvw` / `padding:0` が、
> こちらの `.page{max-width:1920px}` を specificity で上回って無効化していたことだった。
> 詳細と実装内容は2.8章を全面更新済み。
>
> **TODO-2は「実装完了」ではない。** ドキュメントを鵜呑みにして有効化したところ、実際の実装は
> 「元ページを残したまま並べて表示する」という本来の要望を満たしておらず、しかも見た目の副作用
> （タブ積み上げによる紛らわしい表示）もユーザー指摘で発覚した。次に着手する担当は2.8章の訂正部分を先に読むこと。

## 絶対に守る6つのルール

1. **⛔ 教科書の基準はIPA出題範囲だけ。`応用情報単語リスト.md` は教科書の工程では開かない**（→ 2.5章冒頭）。
   単語リストを参照してよいのは**用語集の工程（F1〜F5）に入ってから**
2. **⛔ 教科書が先、用語集が後。** 順序は動かさない。用語集を先に作るのはユーザーの明示指示があるときだけ
3. **想定読者は「情報系大学卒だがIT実務経験なし・基本情報レベル未満」。** たとえ話と「なぜ」を必ず書く
4. **編集は必ずiCloud側（このフォルダ）で行う。** Quartz側の `content/` は上書きされるので直接編集しない
5. **単語リストに無い必要な語は、許可を取らず追加してよい**（→ 8章の追加ルール。**用語集の工程での話**）
6. **モデル分担**：教科書執筆と用語集F1設計・F3レビューはOpus、用語集F2量産はSonnet（→ 2章）

## 既知の罠（過去に実際にやらかしたこと・繰り返さない）

| # | 罠 | 対処 |
|---|---|---|
| 1 | **想定読者を間違えて教科書を書いた** | 執筆前に必ず読者前提を確認。セキュリティは全面書き直しになった |
| 2 | **`sync-content.sh` を忘れて古い状態でビルド**し「反映された」と誤認 | ビルド前に `content/` の中身を確認する |
| 3 | **`grep broken` でリンク切れ0件と誤報告**（Quartzはbrokenクラスを付けない） | 9章の**href実体突合コマンド**を使う（唯一確実） |
| 4 | **`[[SSL/TLS]]` が `ssl/tls` を指してリンク切れ**（`/`がパス区切り扱い） | `[[SSL-TLS\|SSL/TLS]]` 形式で書く（→ 3章） |
| 5 | **教科書の存在しない節番号を13ファイルで参照** | 教科書は**全節に番号を振る**（→ 2.5章）。参照前に見出しをgrepで照合 |
| 6 | **CSSのカスタムがプラグインCSSより先に読まれて効かない** | `body` を前置して詳細度を上げる |
| 7 | **語数の数え間違い**（行数と語数の取り違え／1語漏れ） | 完了前に元リストと `diff` で突合（→ 9章） |
| 8 | **F5でaliasリンクの中身が空になる**（`[[デジタル署名]]`と書くと空のリダイレクトページを指し、ホバー時に何も表示されない。href実体突合では検出できない） | リンクターゲットは常に**実ファイル名**にし、表記は `\|` の後ろに書く（→ 2.6章） |
| 9 | **F5がマークダウンを壊す**（表の中の `[[a\|b]]` は `\|` がセル区切りと解釈され表が崩壊／`**[[語]]**` はCJKで `**` がliteral化） | 2.6章の**F5後マークダウン検証**を必ず行う。表内は `\|` をエスケープ、太字の境界にリンクを置かない |
| 10 | **`var(--highlight)` は黄色**（configの色設定は `@quartz-themes/core` に上書きされており、実体は `rgba(255,208,0,0.4)`） | 表のゼブラ等に `--highlight` を使わない（→ 罠#12の配色設計を使う） |
| 11 | **CSSを直してもCDP実測で変化なし** | ブラウザキャッシュ。`Network.setCacheDisabled` を有効にしてから測る |
| 12 | **表のゼブラを薄くしすぎてヘッダと見分けがつかない**（黄色を消す際に `rgba(128,128,128,0.07)` にしたら、ダークで**ヘッダ40／偶数行35＝差5**になり判読不能に。さらに行の横罫線が `0px` で消えていた） | **背景の微差に頼らず横罫線で行を区切る**。`--tbl-head/zebra/line` をライト・ダーク別に定義し、**ヘッダと偶数行の明度差20前後**を確保する（→ custom.scss の「3. 表を読みやすくする」） |
| 13 | **unlayeredなCSSでも詳細度で負けて無効化される**（`@quartz-themes/core` の `div#quartz-root.page{max-width:100dvw}` はidを含む詳細度(1,1,1)で、こちらの `.page{max-width:1920px}`(0,1,0)を上回っていた。「unlayeredは常に勝つ」は事実だが、**unlayered同士は普通に詳細度勝負になる**ことを見落とし、2026-08-02の「本文の横幅を広げる」実装は一度も効いていなかった） | CSSが効かないときは grep で存在確認するだけでなく、**CDPの `CSS.getMatchedStylesForNode` で実際に勝っているルールとその詳細度・layer所属を直接確認する**。「id込みセレクタ」に対抗するときは罠#6と同じく `body` 等を前置して詳細度を上げる |
| 14 | **サードパーティのQuartzプラグイン（stacked-pages）が前提とする `.page` の余白調整が、当サイトのテーマ上書きで無効化される** | プラグインを`enabled:true`にしただけで終わらせず、**そのプラグインが依存しているCSSの前提（今回は`body.has-binder-left .page{padding-left:40px}`）が実際に効いているかをCDPで確認する**。効いていなければ、プラグインのCSSではなく自サイトの構成要素（今回はサイドバー）側で退避スペースを作る |
| 15 | **`componentResources.css` に渡したCSSはレイアウト指定が効かない**（渡したCSSは `@layer quartz-base` の**内側**、しかも `base.scss` より**前**に置かれるため、`base.scss` の `.sidebar{display:flex}` 等に負ける。popoverのCSSが効くのは`.popover`という競合しないセレクタだからで、たまたま） | **レイアウトに関わるCSSは `custom.scss` から `@use` して読み込む**。custom.scssの出力だけが layer の外（unlayered）に置かれ、レイヤー内の全ルールに勝つ。見た目だけの装飾なら componentResources.css でもよい |
| 16 | **`preventDefault()` だけではQuartzのSPA遷移を止められない**（SPAルータのclickリスナーは `window` にあり、`defaultPrevented` を見ずに自前で`preventDefault`して遷移する） | リンクのクリックを横取りする場合は **`stopPropagation()` も呼ぶ**。バブリングは document → window の順なので、documentで止めればSPA側に届かない |
| 17 | **左サイドバーを `display:none` にすると本文が48pxに潰れる**（`.center` は `grid-area` を持たない**自動配置**のグリッドアイテム。サイドバーをグリッドから消すと自動配置が1つ手前にずれ、`.center` が0px幅の1列目に入る） | サイドバーを隠すときは `display:none` ではなく **グリッドアイテムとして残したまま `visibility:hidden` ＋ 幅0** にする。`visibility:hidden` なら中のリンクもクリック・フォーカスできない |
| 18 | **CDPでスクロール位置を測ると誤検知する**（`html { scroll-behavior: smooth }` があるため、`scrollIntoView()` 直後に `window.scrollY` を読むとアニメーション途中の値を拾う。「クリックで本文が動いた」と誤って判定した） | `scrollIntoView({behavior:'instant'})` を明示し、さらに数百ms待ってから基準値を読む |
| 19 | **日本語文中の太字が太字にならない**（`は**帯域保証型（ギャランティ型）**です` のように書くと、**閉じる `**` の直前が全角括弧・鉤括弧**だと、CommonMarkの規則で閉じ側と認識されず**生の `**` がそのまま表示される**。2026-08-03 ネットワーク教科書の初回デプロイで**14箇所**発生。罠#9はF5が壊すケースだったが、こちらは**執筆時点から壊れている**ため、書いている本人には気づけない） | **執筆時のルール**：太字の**末尾に全角の閉じ括弧（）」』】）を置かない**。`**帯域保証型**（ギャランティ型）` / `「**余裕を残して設計する**」` のように、括弧を太字の外に出すか、文末まで太字に含める。<br>**検出は必ずQuartzの実パーサで行う**（正規表現の自作チェックは誤検出だらけで役に立たない。2026-08-03 に試して断念）。**push前にローカルビルドして確認する**：<br>`cd <Quartz本体> && npx quartz build`<br>`grep -o '\*\*' public/教科書/<分野>.html \| wc -l` が **0** であること。0でなければ壊れている |

## 最初に実行するコマンド

```bash
cd "/Users/yoshidatomohiro/Library/Mobile Documents/iCloud~md~obsidian/Documents/second_brain/002.Notes/002.Privates/勉強/応用情報技術者試験"
ls 教科書/ 用語集/           # 何が完成しているか確認
```

---

## 0. このフォルダの全体像

**目的**：応用情報技術者試験（令和8年度）の合格。現行APは令和8年度で終了するため受験機会は前期・後期の2回のみ。

### ファイル構成

```
応用情報技術者試験/
├─ 00_引き継ぎ.md              ← このファイル（唯一の基準書）
├─ index.md                    ← 公開サイトのトップページ（目次）
├─ 応用情報技術者試験全体像.md    ← 試験制度・学習戦略（調査済み・随時追記）
├─ 応用情報技術者試験_出題範囲詳細.md ← IPA出題範囲の詳細（抜け漏れチェック用）
├─ 応用情報単語リスト.md         ← 998語。毎朝のDaylogルーティンが参照する
├─ プロフェッショナルデジタルスキル試験_調査.md ← 令和9年度以降の新試験の調査
├─ 教科書/                      ← ★メイン成果物（IPA中分類ごと・全23本）
└─ 用語集/                      ← ★教科書の拡張（1単語1ファイル）
```

### 公開サイト（Quartz + GitHub Pages）

- URL：https://tyoshida0413-ai.github.io/ap-exam-textbook/
- Quartz本体：`~/Library/CloudStorage/GoogleDrive-.../マイドライブ/dev/ap-exam-quartz/`
  （**iCloud外に置くのは意図的**。node_modules等の大量ファイルがiCloud同期と干渉するため）
- 反映：`cd <上記> && ./sync-content.sh` の1コマンドのみ
- **編集は必ずiCloud側（このフォルダ）で行う。** Quartz側の `content/` は rsync `--delete` で
  上書きされるコピーなので、直接編集しても消える

### 全体の進捗

| 成果物 | 進捗 |
|---|---|
| **教科書** | **2/23**（11_セキュリティ・10_ネットワークが完成） |
| **用語集** | **140語**（11_セキュリティのみ完成） |

**→ 次にやるのは「10 ネットワークの用語集（F1設計 → F2量産 → F3レビュー → F5リンク）」**（→ 1章の大原則）

---

## 1. 2つの成果物と、その関係

このフォルダの成果物は2つ。**教科書がメインで、用語集はその拡張**という関係にある。

- **教科書**：IPA中分類ごとに1本（全23本）。通読して**体系を理解する**ためのもの
- **用語集**：1単語につき1ファイル。**分からない語を引く**ためのもの

> # ⛔ 大原則：教科書が先、用語集が後（順序は動かさない）
>
> - **教科書がメイン。用語集はそれを拡張するために存在する。**
> - **その分野の教科書が完成していない状態で、用語集を先に作ってはならない。**
>   （ユーザーから明示的な指示があった場合のみ例外）
> - 理由：用語集の各ファイルは「教科書：`[[分野名]]` の N 節」で**教科書へ必ずリンクする**設計になっている。
>   教科書が無ければこのリンクが張れず、**辞書から体系へ戻る導線が失われる**。
>   セキュリティで機能したのは、教科書が先にあったからこの相互リンクが成立したため。
>
> **1分野の正しい進め方（全5工程）**：
> ```
> (1) 教科書/<分野>.md を作る       ← ここが起点。これ抜きで(2)へ進まない【Opus】
>     基準は出題範囲詳細のみ。単語リストは開かない（→ 2.5章冒頭）
>          ↓  ← ★ここから下が「用語集の工程」。単語リストを開いてよいのはここから
> (2) 用語集のF1（設計：難語選定・混同ペア表・見本3〜5本）【Opus】
>          ↓
> (3) 用語集のF2（量産：残り全語）【Sonnet】
>          ↓
> (4) 用語集のF3（難語だけレビュー）【Opus】
>          ↓
> (5) 【F5】教科書→用語集のリンク設定【Sonnet】 → 2.6章
>          ↑ ここまでやって初めて「相互に行き来できる網」が完成する。
>            (4)で終わらせるとリンクが片道のままになる
> ```

### 教科書との役割分担

| | 教科書（`教科書/`）＝**メイン** | 用語集（`用語集/`）＝**拡張** |
|---|---|---|
| 単位 | IPA中分類ごと（全23本） | **1単語ごと** |
| 読み方 | 通読して体系を理解する | **分からない語を引く** |
| 分量 | 1本 15,000〜35,000字 | 1語 **1,000〜1,500字** |
| 役割 | 「なぜそうなるか」の筋道 | その語の**単体での完結した理解** |
| 作る順 | **先** | **後**（教科書に依存する） |

### なぜ1ファイル1単語なのか

Obsidian／Quartz上で `[[SQLインジェクション]]` とリンクすると、
**ホバーで内容がポップアップ表示**され、逆に単語側からは「どの教科書で使われているか」が
**バックリンク**で分かる。孤立した用語集ではなく、**教科書と相互に行き来できる網**にすることが目的。

---

## 2. モデルの分担（重要）

### 全体の割り当て

| 作業 | モデル | 理由 |
|---|---|---|
| **教科書の執筆** | **Opus** | ↓ 2.1で詳述 |
| 用語集 F1（設計） | **Opus** | 難語選定・混同ペアの見極めは横断的判断が要る |
| 用語集 F2（量産） | **Sonnet** | 見本と一行定義がある定型的なコンテンツ生成 |
| 用語集 F3（レビュー） | **Opus** | 難語だけ点検（全件レビューはしない） |

### 2.1 なぜ教科書はOpusなのか（2026-08-02 実証にもとづく判断）

**用語集F2でSonnetが成功した理由**を裏返すと、教科書との差がはっきりする。

| | 用語集F2（Sonnetで成功） | 教科書 |
|---|---|---|
| 手本 | 見本4本＋教科書があった | **無い**（自分が手本を作る側） |
| 元ネタ | 一行定義が既にある | **無い**（分野の体系から組む） |
| 作業の性質 | 定義を膨らませる定型作業 | **章立ての設計＋たとえ話の創出** |
| 分量 | 1語 1,000〜1,500字 | **1本 15,000〜37,000字を一貫した品質で** |

**Sonnetの品質は実際に高い**（F3で難語42語をレビューし、技術的誤りはゼロだった。
MACの「夫婦共有の合言葉スタンプ」のように、教科書に無いたとえ話も自力で作れている）。

それでも教科書をOpusにするのは、**教科書が「手本を作る側」だから**。
用語集F2が成立したのは教科書という基準があったからで、
**その基準自体の品質が落ちると全体が連鎖的に劣化する**。
想定される失敗も「事実誤り」ではなく「**説明が平板になる**」ことであり、
これは想定読者（IT実務経験なし）にとって致命的になる。

> **ただしOpusも完璧ではない。**
> 「教科書の節に番号が無く、用語集13ファイルが存在しない節を参照した」という構造的欠陥は、
> Opusが書いた教科書に起因していた。**モデルに関わらず9章の検証は必須。**

**例外**：付録A（用語チェックリスト）と付録C（NotebookLMプロンプト集）は定型作業のため
Sonnetでも足りる。ただし分割の手間が節約効果を上回るなら、教科書1本を通してOpusで書いてよい。

### 2.2 用語集の3フェーズ

品質差が出るのは一部の用語だけなので、**Opusを使う場面を限定する**。

```
【フェーズ1】Opus … 分野ごとの「設計」
  ├ 難語リストの選定（その分野で品質が効く語を抽出）
  ├ 混同ペア表の作成（どの語とどの語が紛らわしいか）
  └ 難語の見本を3〜5本執筆（基準を示す）
        ↓
【フェーズ2】Sonnet … 量産
  └ 見本に沿って、その分野の残り全語を執筆
        ↓
【フェーズ3】Opus … 限定レビュー
  └ フェーズ1で挙げた「難語」だけを点検する（全件レビューはしない）
```

### なぜこの分担なのか

- 全語の大半は「一行定義を膨らませれば済む」**定型的なコンテンツ生成**。
  グローバルCLAUDE.mdの基準どおり、ここに上位モデルは不要。
- 一方、**たとえ話の質**と**混同ペアの見極め**は横断的な判断が要るためOpusが有効。
- **「全部Sonnetで作ってから全部Opusでレビュー」はしない。**
  数百ファイルを流し読みするレビューは、実質的に追認に終わり、手直しが必要な箇所を
  見落としたまま「レビュー済み」の体裁だけが残るため。

### モデル切り替えのタイミング

Claude側からモデルは変更できない。**フェーズが変わったら、ユーザーに `/model` での切り替えを依頼する。**

---

## 2.5 教科書の作成基準（★メイン成果物）

**唯一の完成例：`教科書/11_セキュリティ.md`（約36,800字）。新しい分野を書くときは必ずこれを読んで踏襲する。**

### 想定読者（全分野で共通・絶対に外さない）

> **情報系の大学は出ているが、IT実務経験はなく、基本情報レベル未満。**

この前提を外すと教科書が成立しない。セキュリティでは、当初この前提を誤って書き始めたため
**全面書き直し（v1.0.0→v2.0.0）**になった。最初に必ず確認すること。

### ⛔ 教科書の工程では単語リストを一切参照しない（2026-08-03 の失敗にもとづく厳密ルール）

> # **教科書の唯一の基準は `応用情報技術者試験_出題範囲詳細.md`（IPA出題範囲）。**
> # **`応用情報単語リスト.md` は教科書の工程では開かない。**
>
> | | 何のためのファイルか | 教科書の工程で使うか | 用語集の工程で使うか |
> |---|---|:---:|:---:|
> | `応用情報技術者試験_出題範囲詳細.md` | **IPA出題範囲**。試験に出る範囲そのもの | **○ これだけを使う** | 参考 |
> | `応用情報単語リスト.md` | **用語集の母集団**／毎朝のランダム出題の材料 | **✕ 開かない** | ○ 使う |
>
> **理由**：目的は「試験範囲の網羅」であって「単語リストの消化」ではない。
> 単語リストは網羅性を保証しない（IPAが定めたものではなく、こちらで用意した語の集合にすぎない）。
> **これを教科書の骨格や完成判定に使うと、出題範囲の抜けが見えなくなる。**
>
> **教科書の完了条件は1つだけ**：
> **その中分類の小分類1〜Nの知識項目例が、1つ残らず本文で説明されていること。**
> 語数・単語リストとの一致率は、**教科書の完成判定には使わない**。

**2026-08-03 のネットワーク教科書で、この手順を踏まずに失敗した。**
単語リストの74語から章割り当て表を作り、それを骨格として執筆し、
完了検証も「74語が全て本文に出現するか」だけで行った。
結果、**IPA出題範囲にあって単語リストに無い項目が丸ごと抜けた**（ユーザーの指摘で発覚）。

抜けた例（中分類10の場合）：**伝送方式と回線**（半二重・全二重／同期・非同期／多重化／誤り制御）、
**回線接続装置**（ONU・モデム・DSU）、**LAN/WANの種類と特徴**、**イントラネット・エクストラネット**、
**通信サービス**（専用線・広域イーサネット・FTTH）、**LTE**、HDLC、CORBA、センサーネットワーク、
障害管理・性能管理。加えて頻出のSTP・リンクアグリゲーション・VRRP・IEEE 802.11規格・HTTP/2/3も欠落した。

**正しい手順**：

```
(1) 応用情報技術者試験_出題範囲詳細.md から、その中分類の小分類1〜Nと知識項目例を抜き出す
        ↓  ← これが唯一の骨格。章立てはこの項目を漏れなく覆う形で作る
(2) 知識項目例を1つずつ「どの章のどの節で説明するか」に割り付けた対応表を作る
        ↓  ← 割り付け先が空欄の項目が残っていたら、章立てが未完成ということ
(3) 執筆（出題範囲に無くても、理解に必要な語・頻出の語は自分の判断で足してよい）
        ↓
(4) 検証：★出題範囲詳細の知識項目例が、1つ残らず本文で説明されているかを項目ごとに突合する
        ↓  ← これに合格したら教科書は完成。単語リストとの照合は行わない
(5) ここまで終えてから、はじめて用語集の工程に入る（そこで初めて単語リストを開く）
```

**教科書を書いている間は、単語リストを開かない。**
用語集の工程に入ったあと、単語リストに無い語が必要になったら、
8章の「単語リストへの追加ルール」で追加してよい（許可不要）。

### 各節の構成（この順で書く）

```
(1) 【まず日常の話】   身近な場面でイメージをつかむ
(2) 【ITだとこうなる】 そのイメージがIT用語のどこに対応するか
(3) 【用語まとめ】     試験に出る形での整理（表）
(4) 【試験ではこう出る】ひっかけ・頻出パターン
```

### 執筆ルール

- **たとえ話を必ず先に置く**（例：公開鍵暗号＝「閉める鍵と開ける鍵が別々の錠前」）
- **略語は正式名称とその意味を書く**（IDS＝Intrusion **Detection** System＝侵入「検知」）
- **「なぜ」を必ず書く**（ログを別サーバへ送る → なぜ？＝管理者自身に消させないため）
- **誤解パターンを明示する**（✕「署名すれば中身も守られる」→ ○「実印を押しても文面は読める」）
- **計算は途中式まで**（鍵の数 100×99÷2＝4,950）
- 丸囲み数字（①②③）は使わない。`(1)(2)(3)` を使う
- **こじつけたたとえ話は書かない**（制度名・組織名など自然な比喩がない語では省略する）

### 必須の構成要素

| 要素 | 内容 |
|---|---|
| 本文 | 分野の体系を通読で理解できる筋道 |
| **科目B対策の節** | 設問パターン・解答の型・そのまま使える解答ストック |
| **付録A 用語チェックリスト** | 想起用（声に出して説明できるか試す） |
| **付録B 想定問題** | 科目A形式（四択10問）＋科目B形式（記述式）。**解答は `<details>` で折りたたむ** |
| **付録C NotebookLM深掘りプロンプト集** | 理解確認／具体例／問題生成／弱点掘り下げ／分野横断の5カテゴリ |

### 分量の目安

| 分野の性質 | 目安 |
|---|---|
| 科目B必須・最頻出（セキュリティ） | 30,000〜37,000字 |
| 科目B選択候補（ネットワーク、DB、PM、監査ほか） | 20,000〜30,000字 |
| 科目A中心 | 15,000〜20,000字 |

> **⚠️ 実績（2026-08-03 ネットワーク）：目安を超えて約46,500字になった。**
> 内訳は本文が約36,000字、付録A〜Cが約10,500字。超過の理由は次の2つ。
> (1) **1語あたりの説明量がセキュリティより厚い**（セキュリティは140語で36,800字＝1語あたり約263字、
> ネットワークは74語で46,500字＝1語あたり約628字）。ネットワークは「なぜそうなるのか」の
> 因果が長い語（3ウェイハンドシェイクがなぜ3回必要か、無線でなぜ衝突検出が使えないか）が多く、
> 想定読者（IT実務経験なし）に届かせるとこの長さになった
> (2) **計算問題の途中式**（サブネット分割・伝送時間）を省略しなかった
>
> **判断**：目安は「読者が通読できる上限」の意味だが、**削るなら本文の因果説明ではなく
> 付録A（想起用チェックリスト）を短くする**のが先。次の分野を書く担当は、
> 目安を超えたときにこの順序で削ること。**因果説明を削って字数を合わせない**（罠#1の再発になる）。

### 章立てのルール（用語集からのリンクに直結する）

- **章は `## N. タイトル`、節は `### N.M タイトル` の形式にする**
- **番号なしの小見出し（`### 【まず日常の話】` など）を章直下に置くと、
  用語集から「N.M 節」で参照できなくなる。**
  セキュリティの0章・1章がこの形になっており、**用語集13ファイルで存在しない節を参照する不具合**が発生した。
  **新しい分野では、全ての節に番号を振ること。**

---

## 2.6 教科書→用語集のリンク設定【F5】（用語集の完成後に行う）

### なぜやるのか

用語集の各ファイルは教科書へリンクしている（用語集→教科書：140本）。
しかし**教科書から用語集へのリンクは、作った直後は0本**である。
理由は制作順で、教科書を書いた時点では用語集がまだ存在しないため。

このままだと**片道**になり、「教科書を読んでいて分からない語があっても用語集へ飛べない」。
1章に掲げた「**教科書と相互に行き来できる網**」が成立しないので、
**用語集がその分野で完成したら、必ずこの作業を行う**。

### 粒度のルール（2026-08-02 実測にもとづき決定）

> ## ✅ **段落ごとに初出1回だけリンクする。表の中も対象にする。**

セキュリティ（36,800字・用語140語）での実測値：

| 粒度 | リンク数 | 判定 |
|---|---:|---|
| 節ごと初出（表を除く） | 58 | 少なすぎる。辞書として機能しない |
| 段落ごと初出（表を除く） | 71 | まだ少ない |
| 節ごと初出（表を含む） | 124 | ─ |
| **段落ごと初出（表を含む）** | **164** | **← これを採用** |
| 全出現をリンク | 352 | 多すぎる。本文がリンクだらけで読めない |

**「段落」の定義**：空行で区切られたかたまり。同じ段落内に同じ語が2回出たら、**最初の1回だけ**リンクする。

### ⚠️ 表の中も必ずリンクする（当初の判断を実測で覆した経緯）

当初は「表の中はリンクしない」と決めかけたが、**これは誤り**だったので撤回した。

- 教科書は**比較表が主役**の構成で、用語の出現352回のうち**97回が表の中**にあった
- 表を除外すると、`IDS`↔`IPS`、`CVE`↔`CWE`↔`CVSS` のような
  **最も引きたくなる対比表からリンクが消える**。辞書として使えなくなる
- 除外しようとした理由は「レイアウト崩れの懸念」だったが、
  **用語集の混同ペア表では既に表内リンクを使っており、公開サイトで正常表示されている**。懸念は成り立たない

### リンクしない場所（除外）

| 場所 | 理由 |
|---|---|
| **見出し行**（`#` で始まる行） | 見出しがリンクだらけになると構造が読めない |
| **コードブロック**（``` で囲まれた範囲） | 図解・ASCIIアートが崩れる |
| **付録すべて**（`## 付録A` 以降） | 特に**付録A 用語チェックリスト**は想起用。リンクがあると答えが見えてしまう。付録B想定問題・付録Cプロンプト集も対象外 |

セキュリティでは、付録に130回の出現があったが、これらはすべて除外した。

### 表記ゆれの扱い

> **厳密にしすぎない。同じものは同じ語として扱い、用語集のファイルは増やさない。**

- 例：**ディジタル署名／デジタル署名** は同一。ファイルは1つだけ作る
- 吸収の方法：用語集ファイルの frontmatter `aliases:` に別表記を書く
  ```yaml
  ---
  title: ディジタル署名
  aliases:
    - デジタル署名
  ---
  ```
- リンクを張るときは `[[ディジタル署名]]` に統一してよい。
  本文の表記を保ちたい場合は `[[ディジタル署名|デジタル署名]]` を使う
- **表記ゆれのたびに新しいファイルを作らない**（語数が無意味に増え、内容も重複する）

### ⚠️ リンクターゲットは必ず「実ファイル名」にする（2026-08-02 実測で発覚した重大な罠）

**当初の実装は失敗した。** `[[表記そのまま]]`で置換すると、次の2つが同時に壊れる。

1. **aliasでリンクすると、ホバープレビューの中身が空になる**
   `[[デジタル署名]]`（実ファイルは`ディジタル署名.md`、`デジタル署名`はalias）と書くと、
   Quartzは`alias-redirects`プラグインにより**`meta http-equiv="refresh"`だけの空リダイレクトページ**を
   ルート直下に自動生成し、リンクはそこを指す。**クリックすれば最終的に正しいページへ着くので
   リンク切れ検査（href実体突合）ではすり抜けてしまう**が、ホバー時にfetchされる中身は
   `<meta http-equiv="refresh">`のみで、辞書の核心である**ホバープレビューが機能しない**。
   （CDPで実測：`article.popover-hint`が存在せず、ポップオーバーに何も表示されないことを確認済み）

2. **titleに`/`を含む語（ISO/IEC 15408、JPCERT/CC、S/MIME）で、3章と同じスラッシュ問題が再発する**
   `/`が実ファイル名では`-`に置換されている（`ISO-IEC 15408.md`）のに、
   titleの表記のまま`[[ISO/IEC 15408]]`と書くと`iso/iec-15408`という存在しないパスを指す。

**正しい実装**：リンクターゲットは常に**用語集ファイルのbasename（拡張子を除いたファイル名）**にし、
表記がbasenameと異なる場合（alias・別表記）だけ`[[basename|見つかった表記]]`にする。

### 実行手順

**担当モデル：Sonnet**（→ 2.7）

1. 対象分野の用語一覧を、**「ファイル名(basename)」→「title・aliasesの全表記」**の形で取得する
   （表記だけを集めてリンクターゲットにしてはいけない。ファイル名を主キーにする）
2. **長い語から順に**処理する（`公開鍵暗号方式` を先に処理しないと `公開鍵` が誤爆する）
3. 上記ルールで `[[basename]]` または `[[basename|表記]]` に置換する
4. **検証**（→ 下記4項目。特に(4)は必須）

参考実装（実行後は必ず4の検証を行うこと。ファイル名を主キーにしている点が最重要）：

```bash
cd "/Users/yoshidatomohiro/Library/Mobile Documents/iCloud~md~obsidian/Documents/second_brain/002.Notes/002.Privates/勉強/応用情報技術者試験"
python3 - <<'PY'
import re, glob, os
FIELD = "11_セキュリティ"          # ← 対象分野に変える
BOOK  = f"教科書/{FIELD}.md"

# term_to_slug: 表記(title自身 or alias) -> 実ファイル名(=正しいリンクターゲット)
term_to_slug = {}
for f in glob.glob(f"用語集/{FIELD}/*.md"):
    base = os.path.splitext(os.path.basename(f))[0]   # ★ 実ファイル名を主キーにする
    if base.startswith("00_"): continue
    fm = open(f, encoding='utf-8').read().split('---')[1]
    title=None; aliases=[]; in_aliases=False
    for line in fm.split('\n'):
        if line.startswith('title:'): title=line[6:].strip()
        elif line.startswith('aliases:'): in_aliases=True
        elif line.startswith('  - ') and in_aliases: aliases.append(line[4:].strip())
        elif line and not line.startswith(' '): in_aliases=False
    if title:
        term_to_slug[title] = base
        for a in aliases: term_to_slug[a] = base

terms = sorted(term_to_slug.keys(), key=len, reverse=True)   # 長い語から（誤爆防止）

src = open(BOOK, encoding='utf-8').read().split('\n')
appendix = next((i for i, l in enumerate(src) if l.startswith('## 付録')), len(src))

out=[]; infence=False; para=0; prev_blank=True; seen=set()
for i, l in enumerate(src):
    if l.strip().startswith('```'): infence=not infence; out.append(l); continue
    if l.strip()=='': prev_blank=True; out.append(l); continue
    if prev_blank: para+=1
    prev_blank=False
    if i>=appendix or infence or l.startswith('#'):
        out.append(l); continue            # 付録・コード・見出しは除外（表は対象）
    for t in terms:
        slug = term_to_slug[t]
        k=(slug, para)                      # ★ 実ファイル名単位で重複判定（表記違いでも同一ページなら1回）
        if k in seen: continue
        parts=re.split(r'(\[\[[^\]]*\]\])', l)
        done=False
        for j,p in enumerate(parts):
            if p.startswith('[['): continue
            if t in p:
                repl = f'[[{slug}]]' if t==slug else f'[[{slug}|{t}]]'   # ★ 表示は元の表記のまま
                parts[j]=p.replace(t, repl, 1); done=True; break
        if done:
            l=''.join(parts); seen.add(k)
    out.append(l)
open(BOOK,'w',encoding='utf-8').write('\n'.join(out))
print(f"リンク設定完了: {len(seen)} 箇所")
PY
```

### 検証（必須）

```bash
# (1) リンク数が想定の範囲か
grep -o '\[\[[^]]*\]\]' 教科書/<分野>.md | wc -l

# (2) 見出し・コードブロック・付録に混入していないか
grep -n '^#.*\[\[' 教科書/<分野>.md          # 見出しに入っていたらNG
awk '/^## 付録/{f=1} f && /\[\[/{print NR": "$0}' 教科書/<分野>.md   # 付録に入っていたらNG

# (3) 入れ子の壊れがないか（[[[[ や ]]]] が無いこと）
grep -n '\[\[\[\|\]\]\]' 教科書/<分野>.md

# (4) リンク切れ検査（9章の href 突合コマンドを教科書にも適用する）

# (5) ★最重要：aliasリンクが実体ページを指しているか（href実体突合だけでは検出できない）
grep -o '<a[^>]*>[^<]*</a>' 教科書/<分野>.html | grep 'alias' | head -20
# → href が "../用語集/<分野>/xxx" のように用語集フォルダを経由していればOK
# → href が "../xxx"（用語集フォルダを経由しない）だった場合、alias-redirects の空リダイレクトを
#   踏んでいる。ホバープレビューが機能しないので、実行手順のスクリプトが正しく動いていない
```

### ★ (6) F5後マークダウン検証（2026-08-02 に見落として実害が出た）

**リンクを入れるとマークダウンが壊れる。** リンク数やリンク切れをいくら確認しても検出できない。
**ビルド後のHTMLに記法が literal で残っていないか**を必ず確認する。

```bash
# 描画結果に [[ や ** が生テキストで残っていたら壊れている
cd "<Quartzのpublic>"
python3 -c "
import re,html
h=open('教科書/<分野>.html',encoding='utf-8').read()
m=re.search(r'<article.*?</article>',h,re.S)
b=html.unescape(re.sub(r'<[^>]+>','',m.group()))
print(\"literal '[[' :\", b.count('[['))
print(\"literal '**' :\", b.count('**'))
"
# → いずれも 0 でなければ壊れている

# 内容が消えていないかもF5前のバックアップと照合する（実行前に必ずcpを取る）
```

**壊れる3パターンと対処**：

| パターン | 何が起きるか | 対処 |
|---|---|---|
| 表の中の `[[a\|b]]` | `\|` が**セル区切り**と解釈され表が崩壊する | 表の行では `\|` を `\\\|` にエスケープする |
| `**[[語]]**` | 太字の中身がリンクだけ。CJK文脈で `**` がliteral化 | 外側の `**` を外す（リンク自体が十分目立つ） |
| `**前置き[[語]]**` / `**[[語]]後続**` | 太字の**境界にリンクが接する**とliteral化 | リンクを太字の外に出す（`**前置き**[[語]]`） |

> **なぜ起きるか**：CommonMarkの emphasis は「左/右フランキング」条件で判定される。
> 閉じ `**` の直前が `]` や全角の `）` のような**約物**だと右フランキング条件を満たさず、
> 太字として解釈されずに `**` がそのまま表示される。日本語文書で特に起きやすい。

**(4)(5)(6) は必ず行う。** (4)だけでは、alias-redirectsが自動生成する「実体を持つがほぼ空のリダイレクトページ」
を**リンク切れとして検出できない**（実ファイルとしては存在するため）。
2026-08-02の作業で、当初の実装がこの罠にはまり、`[[デジタル署名]]`（alias）のようなリンクが
**クリックすれば正しいページへ着くのにホバーでは何も表示されない**という不具合を作り込んだ。
(4)は「リンク切れではない」ことしか確認できず、「ホバーが機能するか」は別に確認する必要がある。

---

## 2.7 リンク設定はどのモデルでやるか

> **Sonnetで十分。**

- リンク先は**既に確定している**（用語集のファイル群）。設計判断が要らない
- やることは「本文中の語を `[[ ]]` で囲む」**機械的な作業**
- 正誤が**機械的に検証できる**（上記の検証コマンド）
- 手本もある（用語集→教科書のリンク方式）

用語集F2（Sonnetで成功した量産作業）と同じ性質。
**ただしルール（粒度・除外・表記ゆれ）はこの2.6章で確定済みであることが前提。**
ルールが未確定のまま渡すと、リンクだらけの読みにくい教科書になる。

---

## 2.8 サイト表示の改善2件（2026-08-02 調査 → 2026-08-03 実装・検証済み）

> **状態：実装・CDP実測での検証まで完了。ただし `git push`（本番デプロイ）はまだしていない。**
> ユーザーへ差分を報告し、pushの許可を得てから `git add/commit/push` を行うこと（Quartzリポジトリはコード変更＝公開サイトに直結するため、コンテンツ編集と違い明示確認が要る）。

### TODO-1：高解像度モニターで本文が広がらない

**症状（2026-08-02時点）**：画面を広げても本文が930pxのまま。余った空間がすべて余白として捨てられる。

**2026-08-02の原因分析は誤りだった。** 当初は「`base.scss` の `.page{max-width:calc(1200px+300px)}`＝1500pxで頭打ち」と推測したが、
2026-08-03にヘッドレスChrome+CDPの `CSS.getMatchedStylesForNode` で実際に勝っているルールを確認したところ、**全く別の原因**だった。

**真因（2026-08-03 CDP実測で確定）**：
`@quartz-themes/core`（`node_modules/@quartz-themes/core/dist/index.js`）が `(min-width:1200px)` で
以下を**unlayered**（`@layer` に属さない）で注入している。

```css
/* @quartz-themes/core のCSS（コメントも原文どおり） */
div#quartz-root.page {
  /* workaround for reader-mode */
  display: grid;
  place-items: center;
  width: 100%;
  min-width: 100dvw;
  max-width: 100dvw;   /* ← .pageは常に画面幅いっぱい。custom.scssの max-width:1920px は無意味だった */
  padding: 0;
  margin: 0;
}
div#quartz-root.page div#quartz-body {
  width: min(100%, calc(1200px + 300px));      /* ← 本当の頭打ちはこっち。1500px */
  max-width: min(100%, calc(1200px + 300px));
  padding: 0 auto;
}
```

CSSのカスケードレイヤー仕様上、unlayeredなルールはlayered（`@layer quartz-base`内）のルールに常に勝つ。
`custom.scss` の出力もunlayeredだが、セレクタの詳細度で負けていた：
`.page`（詳細度 0,1,0）< `div#quartz-root.page`（id1つ分＝1,1,1）。
`#quartz-body` 側も同様に、こちらの `.page > #quartz-body{grid-template-columns:...}`（詳細度 0,2,0）が
`div#quartz-root.page div#quartz-body{max-width:min(100%,1500px)}`（詳細度 2,1,2）に負けていた。
**「本文の横幅を広げる」セクションは、書かれた当初から一度も効いていなかった。**

**決定済みの方針（ユーザー選択・変更なし）**：
> **画面幅に応じて可変・上限1400px**

**実装内容（2026-08-03・`quartz/styles/custom.scss` セクション1を全面書き換え）**：
罠#6と同じ「詳細度を上げて上書きする」パターンで、本当の頭打ち元である `#quartz-body` 側を、
同等以上の詳細度（`body` を前置）で上書きした。

```scss
$sidePanelWidthNarrow: 280px;

@media all and #{$desktop} {
  .page > #quartz-body {
    grid-template-columns: #{$sidePanelWidthNarrow} auto #{$sidePanelWidthNarrow};
  }

  body div#quartz-root.page div#quartz-body {
    width: min(100%, calc(1400px + #{$sidePanelWidthNarrow} * 2 + 10px));       // = min(100%, 1970px)
    max-width: min(100%, calc(1400px + #{$sidePanelWidthNarrow} * 2 + 10px));
  }
}
```

**検証結果（2026-08-03、CDP実測・`Emulation.setDeviceMetricsOverride` + `getBoundingClientRect`）**：

| 画面幅 | #quartz-body幅 | 本文（center）幅 | 判定 |
|---:|---:|---:|---|
| 1440 | 1440（可変） | 870 | ✅ 可変域 |
| 1920 | 1920（可変） | 1350 | ✅ 可変域 |
| 2560 | 1970（頭打ち） | **1400** | ✅ 上限どおり |
| 2810 | 1970（頭打ち） | **1400** | ✅ 上限どおり |
| 3440 | 1970（頭打ち） | **1400** | ✅ 上限どおり |
| 600〜1199（モバイル・タブレット） | 変更前と同じ | 変更前と同じ | ✅ 回帰なし（`@media $desktop` の外なので無変更） |

スクリーンショットでも崩れなし（左右サイドバー280px・グラフ/目次/バックリンクの表示は変更前と同一）。

### TODO-2：用語クリックでサイドパネルに用語ページを表示（Stacked Pages）

**ユーザーの要望**：「用語をクリックしたときに、サイドパネルに当該用語のページを表示させる」

**実装（2026-08-03）**：`quartz.config.yaml` の `@quartz-community/stacked-pages` を `enabled: true` にし、
ドキュメント記載の既定オプション（`maxTabs:8` / `mobileBreakpoint:800` / `showSpines:true` / `animateTransitions:true`）を明記した。
プラグイン本体は**既にインストール済み**だった（2026-08-02時点の「本体未インストール」というメモは誤り。`node_modules/@quartz-community/stacked-pages` に実体あり）。

**実際の挙動（CDPでクリックを実際にシミュレートして確認。ドキュメントの説明とは細部が異なる）**：
- 内部リンクをクリックすると、Quartz標準のSPAルータ（`window.spaNavigate`）で**通常どおりページ遷移**する
  （ドキュメントにある「遷移せず横に並べて開く」ような複数ページ同時表示ではない）
- 遷移後、`document.addEventListener("nav", ...)` で発火する更新処理が、画面端に固定表示の
  **「バインダー」帯**（`#stacked-pages-container.binder-active`、`position:fixed; inset:0` の全画面オーバーレイ内に
  幅40pxの `.binder-strip-left`）を作り、訪問したページを縦書きタブとして積み上げる
- タブをクリックすると該当ページへ`spaNavigate`で戻る、×ボタンでタブを閉じられる
- `body` に `has-binder-left` / `has-binder-right` クラスが付く。800px未満では `display:none` で自動的に無効（確認済み）

**⚠️ 有効化しただけでは左サイドバーと重なるバグがあった（2026-08-03 発見・修正済み）**：
プラグイン本来の設計は `body.has-binder-left .page { padding-left: 40px }` で本文を40px退避させるものだが、
TODO-1と全く同じ原因（`@quartz-themes/core` の `div#quartz-root.page{padding:0}` がidの詳細度で勝つ）でこの退避が無効化されており、
`#quartz-body` が画面幅いっぱいになる範囲（およそ1200〜1970px＝1440pxや1920pxなど一般的な解像度を含む）では、
40px幅のバインダー帯が左サイドバー（エクスプローラー）の文字にそのまま重なって読めなくなっていた
（`.page` 側で退避が効かないのはTODO-1と同根の問題）。

**対処（`custom.scss` にセクション4として追記）**：`.page` 側の詳細度勝負を避け、サイドバー自身の内側に余白を作る方式にした。

```scss
body.has-binder-left .sidebar.left {
  padding-left: calc(2rem + 40px);
}
body.has-binder-right .sidebar.right {
  padding-right: calc(2rem + 40px);
}
```

**🛑 2026-08-03（同日中）訂正：そもそもユーザーの要望を満たしていないことが判明。ユーザー指摘で発覚、勝手な追加実装はしていない。**

上の「実際の挙動」に書いたとおり、このプラグインは**通常のページ遷移＋訪問履歴タブ**を作るだけで、
**元のページを残したまま該当ページを並べて表示する機能は無い**。`docs/plugins/StackedPages.md` の
「遷移せず右へ並べて開く」「各ペインは個別にスクロールできる」という説明は、実装（`node_modules/@quartz-community/stacked-pages`）と一致しない。
このパッケージは`README.md`の中身が汎用プラグインテンプレートのままで、専用の実装ドキュメントではなかった。

#### 実装ソース全文読了による確証（2026-08-03・ユーザーの再確認指示を受けて実施）

当初この結論はコードの断片的なgrepだけを根拠にしていた。ユーザーの指示で、npm配布物とGitHubソースの**両方を全文確認**し、以下を確定させた。

| 確認項目 | 結果 |
|---|---|
| npm公開バージョン | **`0.1.0` のみ**（`npm view ... versions`）。より新しい実装が別バージョンに存在する可能性は無い |
| npm公式description | **`Binder-style stacked page tabs for Quartz v5.`**（ペインではなく「**タブ**」と明記） |
| keywords | `binder` / `tabs`（`pane`・`sliding` は無い） |
| 実装本体（`dist/components/index.js` および GitHub `src/components/scripts/stacked.inline.ts`） | `fetch`・`DOMParser`・他ページ本文の取得/描画処理が**一切存在しない**。`createElement` で生成するのは strip / tab / spine / label / closeBtn のみ |
| 内部リンクのクリック横取り | **していない**。`a.internal` へのリスナー登録が無く、遷移はQuartz標準のSPAルータ任せ。プラグインは `nav` イベントを受けて履歴を記録するだけ |
| タブのクリック時 | `window.spaNavigate` で**ページ全体が入れ替わる**（並べて表示されない） |
| docs記載の `#stacked=slug1,slug2` | **実装に存在しない**。実体は sessionStorage の `stacked-pages-state` キー |
| docs記載の「背表紙に折りたたむ」 | CSSが `.binder-spine { display: none }` のため、`showSpines` オプションは**視覚的に無効**（要素は作られるが常に非表示） |
| npmとGitHubの同一性 | `repository` は `github.com/quartz-community/stacked-pages`。両者の実装は一致 |

> **結論：`docs/plugins/StackedPages.md` の記述は、出荷されているプラグインの実装と一致していない。**
> 要望「サイドパネルに用語ページを表示」は、**このプラグインでは実現できない**（オプション調整では解決しない）。

#### この件で私（Claude）が引き継ぎファイルの指示に違反した点

2.8章のTODO-2「着手時の注意」には次が明記されていたが、いずれも守られなかった。

| 引き継ぎファイルの指示 | 実際 |
|---|---|
| 「教科書1本に189リンクあるため、`maxTabs` の既定8が適切か**実際に触って確認する**」 | **未実施**のままデプロイした |
| 「挙動が大きく変わるので、有効化後は**必ずスクリーンショットで確認**してからデプロイする」 | 2ページ遷移のみの浅い確認で済ませ、実質的に未達だった |
| 仕様欄に「**仕様（ドキュメントより）**」と出典が明記され、未検証と読み取れる書き方だった | 検証済みの事実として扱い、実装コードでの裏取りをしなかった |

**教訓（次の担当者へ）**：このフォルダのQuartz関連作業では、**外部プラグインのドキュメントを実装の根拠にしない**。
`node_modules/` 配下の実体か GitHub の `src/` を読んで、要望を満たすかどうかを先に確定させてから有効化・デプロイすること。

**さらに実運用に近い検証（Home→用語集→11_セキュリティ→機密性の4段階遷移）で発覚した副作用**：
訪問したページ全部がタブとして左端に積み上がるため、最初に訪れた「Home」タブのラベルに**サイト名そのもの**
（「応用情報技術者試験 学習ノート」）が入り、すぐ右にあるエクスプローラーの見出し（同じサイト名）と
紛らわしく重なって見える見た目になっていた（物理的な文字の重なりではなく、同じ文字列が縦書き・横書きで隣接する紛らわしさ）。
これはユーザーが実際のブラウザで発見し指摘するまで、こちらのCDP検証（2ページ遷移のみ）では検出できなかった。

**現在の状態（2026-08-03時点）**：
- 上記の状態のまま**本番に公開済み**（`enabled: true` のままpush・デプロイ済み）
- **要望を満たしていない機能を有効化したままにしている**ため、対応方針をユーザーに確認中
- 対応（無効化に戻す／表示を調整する等）は**ユーザーの指示があるまで実施しない**（このファイルの絶対ルールに反して2.8章を「検証済み」と誤記載したまま完了報告したことが今回の問題。次に着手する担当は、このセクションの「実際の挙動」を鵜呑みにせず、まず実装コード自体を読むこと）

修正後、CDPで同じ操作を再現し、エクスプローラーの文字が隠れずバインダー帯と重ならないことを screenshot で確認済み。

**設定項目（変更なし・既定値のまま採用）**：

| オプション | 値 | 内容 |
|---|---|---|
| `maxTabs` | 8 | 同時に保持するタブの最大数 |
| `mobileBreakpoint` | 800 | この幅未満で無効化（確認済み） |
| `showSpines` | true | 折りたたみ表示（8件超過時。未検証） |
| `animateTransitions` | true | 開閉アニメーション |

**未検証・今後の課題**：
- `maxTabs`（8件）を超えたときの折りたたみ（spine）表示は未確認。教科書1本に189リンクあるため実運用で早めに到達する可能性がある
- 右側バインダー（`has-binder-right`）は発生条件を確認していない（今回のテストは左側のみ）

---

## 2.9 用語サイドパネル（TODO-2の作り直し・2026-08-03 設計確定）

> **これがTODO-2の正式な実装方針。** 2.8章のstacked-pagesは要望を満たさないため破棄し、自作する。
> ユーザー承認済みの設計。

### 満たすべき要件（ユーザーの言葉）

> 「教科書を読みながら、わからない用語が右に表示されて**読み進められる**」
> 「それぞれが**独立**していないと意味がない」
> 「画面外をクリックしたことで閉じてしまうような仕様は**問題外**」

**つまり教科書とパネルは同時に生きていること**が要件の核心。片方を触るともう片方が閉じる／隠れる実装は不可。

### 実装方式：ホバープレビュー（popover）と同じ仕組みを流用する

Quartzのpopoverは既に「ページをfetchして `.popover-hint` を取り出して表示」しており、必要な処理が揃っている。

- `.popover-hint` は**タイトル部の`div`と本文の`<article>`の両方**に付いている（＝用語ページの中身が丸ごと取れる）
- `fetchCanonical()` は**alias用の空リダイレクトページを自動追跡して実体へ到達する**（罠#8の問題が原理的に起きない）
- `normalizeRelativeURLs()` で取得先ページの相対リンクを正しく張り直せる

### レイアウト設計（幅の問題をどう解いたか）

当初「右端にオーバーレイ」で提案したが、**1440pxでは本文を約200px覆うため成立しない**と実測で判明（用語ページは表を含むため最低450px前後必要で、本文を覆わずに重ねる余地がない）。
ユーザー提案により、**左サイドバー（エクスプローラー）を畳めるようにする**ことで解決した。

パネルはオーバーレイではなく**グリッドの1列**として組み込み、右サイドバーの枠を引き継ぐ。

| 画面幅 | 現在（パネルなし） | パネル＋左そのまま | **パネル＋左を畳む** |
|---:|---:|---:|---:|
| 1440 | 870px | 670px | **955px** |
| 1920 | 1350px | 1150px | **1400px** |
| 2560 | 1400px | 1400px | **1400px** |
| 3440 | 1400px | 1400px | **1400px** |

→ **左を畳めば、パネルを開いても本文は現在より広い**（1440pxで870→955px）。

### 動作仕様

**左サイドバーの開閉**
- 畳む／戻すボタンを常時設置。パネルとは無関係に手動で切り替える
- 状態は localStorage に保存し次回も維持
- **自動では畳まない**（勝手に動くと戸惑うため）

**サイドパネル**
- **本文（`article`）内の内部リンクのみ**をパネルで開く。パンくず・エクスプローラー・目次・バックリンクは従来どおり遷移する（区別しないと「Home」までパネルで開いてしまう）
- 開いてもURLは変わらず、本文もスクロール位置も動かない
- パネル内のリンクをクリックするとパネルの中身が差し替わる（本文は動かない）
- **閉じるのは × ボタンと Esc のみ。画面外クリックでは閉じない**（要件）
- Ctrl・Cmd・中クリックは横取りせず、新しいタブで開く
- 800px未満では無効化し通常遷移
- パネルを開いている間、グラフビュー・目次・バックリンクは隠れる（閉じれば復帰）

**意図的に作らないもの（YAGNI）**：タブ、戻る履歴、複数パネルの同時表示

### 変更ファイル

| ファイル | 内容 |
|---|---|
| `quartz/components/scripts/sidepanel.inline.ts` | 新規・パネル本体 |
| `quartz/components/styles/sidepanel.scss` | 新規・パネルの見た目 |
| `quartz/components/scripts/sidebar-toggle.inline.ts` | 新規・左サイドバー開閉 |
| `quartz/components/styles/sidebar-toggle.scss` | 新規・トグルの見た目 |
| `quartz/plugins/emitters/componentResources.ts` | 上記4点の注入（`addGlobalPageResources` 内、popoverと同じ場所） |

併せて `stacked-pages` を `enabled: false` に戻し、不要になる `custom.scss` セクション4（バインダー帯の余白対策）を削除する。

> **なぜプラグイン化しないか**：ローカルプラグイン（`./path` 形式）は公式にサポートされているが、
> package.json一式とtsupビルドが必要で重い。機能的に最も近いpopoverがcoreに直接置かれている以上、同じ場所に置くのが素直。

### 検証項目（前回の失敗を踏まえ必須）

- 教科書→用語リンクのクリックでパネルが開き、**URLが変わらない**こと
- **教科書とパネルが独立して**スクロール・クリックできること（要件の核心）
- パネル内リンクで中身が差し替わること
- Esc / × で閉じ、**画面外クリックでは閉じない**こと
- Ctrl+クリックが壊れていないこと
- 左サイドバーの開閉が効き、リロード後も維持されること
- **4段階遷移（Home→用語集→11_セキュリティ→機密性）** で破綻しないこと ← 前回これを飛ばして事故った
- 1440 / 1920 / 3440px、**ライト・ダーク両方**でスクリーンショット確認

### 実装・検証結果（2026-08-03 完了・**デプロイ済み**）

**ローカルで全23項目合格 → デプロイ後、本番環境でも全41項目合格**
（本番：1440px 18項目／1920px 18項目／共通5項目。ローカル：1440/1920/3440px の各18項目＋共通5項目）。

> 前回（stacked-pages）はローカルの浅い検証だけでデプロイして事故ったため、
> **今回は本番URLに対しても同じ検証スクリプトを流して確認した**。
> 検証スクリプトは `VERIFY_BASE` 環境変数でローカル／本番を切り替えられる作りにしてある。

| 画面幅 | パネルなし | パネル開・左そのまま | パネル開・左を畳む |
|---:|---:|---:|---:|
| 1440 | 870px | 670px | **950px** |
| 1920 | 1350px | 1150px | **1400px** |
| 3440 | 1400px | 1400px | **1400px** |

→ **左を畳めばパネルを開いても本文は元より広い**（1440pxで870→950px）。設計どおり。

確認できたこと：パネルが本文を覆わない／URLとスクロール位置が変わらない／パネルと本文が独立してスクロールできる／本文をクリックしても閉じない／パネル内リンクで中身が差し替わる／Escと×で閉じる／パンくずは通常遷移する／Ctrl+クリックが壊れていない／800px未満で無効化される／4段階遷移で破綻しない／旧stacked-pagesのタブが残っていない。

**実装中に踏んだ罠は #15〜#18 として記録済み**（特に #15 と #17 はレイアウトが壊れる致命的なもの）。

**実装ファイル（設計どおり5点）**：
`components/scripts/sidepanel.inline.ts` / `components/styles/sidepanel.scss` /
`components/scripts/sidebar-toggle.inline.ts` / `components/styles/sidebar-toggle.scss` /
`plugins/emitters/componentResources.ts`（スクリプトのみ注入。**CSSは custom.scss から @use**＝罠#15）
＋ `custom.scss` セクション1を CSS変数方式に整理、セクション4を削除、`quartz.config.yaml` で stacked-pages を無効化。

---

## 3. フォルダ構成と命名

```
応用情報技術者試験/
├─ 00_引き継ぎ.md               ← このファイル
├─ 応用情報単語リスト.md         ← 元ネタ（998語・「用語 | 説明」形式）
├─ 教科書/
│   └─ 11_セキュリティ.md         ← 完成（唯一の見本）
└─ 用語集/
    └─ 11_セキュリティ/           ← 完成（140ファイル）
        ├─ 00_混同しやすい語の一覧.md  ← F1の設計成果物 兼 学習用の対比表
        ├─ SQLインジェクション.md
        ├─ ソルト.md
        └─ …
```

### 命名ルール

- **フォルダ名の番号は、IPAの中分類番号に合わせる**（`教科書/11_セキュリティ.md` と `用語集/11_セキュリティ/` が対応する）
  - ただし `01` と `02` は単語リストの区分と中分類が完全には一致していない（両方が中分類2に一部かかる）。実害がないため、この近似を許容する
- **ファイル名は用語そのまま**（`SQLインジェクション.md`）。番号は付けない
  - 理由：`[[SQLインジェクション]]` とリンクしたときにファイル名がそのまま一致するため
- 記号を含む用語は、ファイル名に使えない文字だけ置換する
  - `/` → `-`（例：`JPCERT-CC.md`、`SSL-TLS.md`）
  - 本文の見出し（`# `）には**元の表記をそのまま書く**（`# JPCERT/CC`）

> **⚠️ `/` を含む用語へのリンクは必ずファイル名形式で書く**（2026-08-02 に実際に発生した不具合）
> `[[SSL/TLS]]` と書くと、Quartzは `/` を**パス区切り**と解釈し `ssl/tls` のような
> **存在しない階層**を指してリンクが切れる。表示テキストを保ちたい場合は次の形式を使う：
> ```
> ✕ [[SSL/TLS]]              → ssl/tls を指してリンク切れ
> ○ [[SSL-TLS|SSL/TLS]]      → 正しく解決し、表示は「SSL/TLS」のまま
> ```
> 対象：`SSL-TLS` / `JPCERT-CC` / `ISO-IEC 15408`
- 別名がある語は frontmatter の `aliases` に書く（`` `[[XSS]]` `` でも `` `[[クロスサイトスクリプティング]]` `` でも飛べるようにする。
  ただし2.6章の罠#8のとおり、**教科書からリンクを張るときは alias 表記そのままではなく実ファイル名を主キーにすること**）

---

## 4. 1ファイルのテンプレート（厳守）

```markdown
---
title: SQLインジェクション
aliases:
  - SQLi
分野: 11_セキュリティ
難度: 難          # 易 / 難 のいずれか（→ 6章の基準）
---

# SQLインジェクション

> 入力欄にSQL文の断片を混ぜ込み、データベースに意図しない命令を実行させる攻撃。

## 日常のたとえ

（身近な場面に置き換える。ITの前提知識ゼロでも情景が浮かぶこと）

## なぜそうなるのか

（仕組み・原理。「なぜこの問題が起きるのか」「なぜこの対策が効くのか」）

## 混同しやすい語

| 語 | 決定的な違い |
|---|---|
| [[クロスサイトスクリプティング]] | あちらは情報を「盗む」／こちらは操作を「実行させる」 |

## 試験での問われ方

- 「対策として適切なものはどれか」→ **プレースホルダ**（エスケープは次善策）
- ひっかけ：〜

## 関連

[[WAF]] ／ [[セキュアプログラミング]] ／ [[プレースホルダ]]

教科書：[[11_セキュリティ]] の 2.2 節
```

### 見本ファイル（執筆前に必ず読むこと）

書式の基準はこの4本。**型が違うものを選んである**ので、書く語に近いものを真似る。

| 見本 | 型 | 何の見本か |
|---|---|---|
| `用語集/11_セキュリティ/ディジタル署名.md` | 難 | **鍵の向き**のような取り違えを、対比表と原則で潰す型 |
| `用語集/11_セキュリティ/ソルト.md` | 難 | **なぜ効くのか**を、攻撃側の動機から順に説明する型 |
| `用語集/11_セキュリティ/DMARC.md` | 難 | **必要になった順**で因果を追う型（3語セットの一員） |
| `用語集/11_セキュリティ/NISC.md` | 易 | **たとえ話・混同ペアを省略した型**。制度名・組織名はこれでよい |

### セクションの増減ルール

- **「日常のたとえ」は、無理なら省略してよい**。こじつけたたとえ話は理解を妨げるため、
  自然な比喩が思いつかない語（`CVE`、`NISC` など固有名詞・制度名）では書かない
- **「混同しやすい語」は、実際に紛らわしい相手がいるときだけ**置く。埋めるために無理に作らない
- 逆に、**「一言定義」「関連」は全語で必須**

---

## 5. 執筆ルール

### 文体・レベル

- **想定読者：情報系の大学は出ているが、IT実務経験はなく基本情報レベル未満**
  （教科書 `11_セキュリティ.md` と同じ前提。あの文体をそのまま踏襲する）
- 専門用語を説明に使うときは、**その場で一言添えるか `[[リンク]]` を張る**
- 略語は初出時に**正式名称と、その意味**を書く（例：IDS＝Intrusion **Detection** System＝侵入「検知」）
- 丸囲み数字（①②③）は使わない。`(1)(2)(3)` を使う

### リンク

- 関連語は積極的に `[[用語名]]` で繋ぐ。**まだ存在しないファイルへのリンクでもよい**
  （後から作ったときに自動で繋がる。むしろ「作るべき語」の目印になる）
- **教科書へのリンクを必ず1本入れる**（`[[11_セキュリティ]]` のように）。
  辞書から体系に戻れる導線がこのプロジェクトの要

### やってはいけないこと

- **元の単語リストの一行説明をコピーしただけで済ませない。** 膨らませることが目的
- **教科書の文章をそのまま貼らない。** 辞書として単体で完結する書き方に組み直す
- **不確かな内容を断定で書かない。** 確信が持てない場合はWeb検索で裏を取る
- **`用語集/` を Quartz側（Googleドライブ）で編集しない**（→ 8章）

---

## 6. 難語の選定基準（フェーズ1で使う）

その分野の語を、次の基準で **易 / 難** に振り分ける。
**難と判定した語だけがフェーズ3（Opusレビュー）の対象**になる。

### 「難」に分類する語

- **たとえ話がないと理解できない**（公開鍵暗号、ソルト、ハイブリッド暗号）
- **鍵・向き・順序を取り違えやすい**（ディジタル署名の秘密鍵／公開鍵）
- **似た語と対で問われる**（IDS↔IPS、SPF↔DKIM↔DMARC、FAR↔FRR、27001↔27002）
- **「なぜ必要になったか」の因果が要る**（DMARC、ゼロトラスト、多要素認証）
- **計算式が絡む**（鍵の数 n(n−1)/2、稼働率、ALE＝SLE×ARO）

### 「易」に分類する語

- 制度名・組織名・規格名で、**定義を書けば足りる**（NISC、ISMAP、PCI DSS、CVE）
- 一般語に近く、説明が一意に定まる（情報資産、内部不正）

**目安：難は全体の2〜3割程度**

> **実績（2026-08-02 セキュリティ）**：138語中 **難42語（30%）／易96語**。
> 当初の想定（1〜2割）より多かった。セキュリティは対比で問われる語が特に密集する分野のため、
> 他分野ではこれより下がる可能性が高い。**行数と語数を取り違えない**こと
> （1行に複数語をまとめると集計がずれる。正はファイルの frontmatter `難度:`）。

---

## 7. 進捗表

作業したら**必ずこの表を更新する**。

> **⚠️「語数」列は用語集（単語リスト）の指標であって、教科書の完成度とは無関係。**
> 教科書の完成判定は出題範囲詳細との突合のみで行う（→ 2.5章冒頭・9-0章）。

| # | 分野（フォルダ名） | 語数 | 教科書 | F1 設計 | F2 量産 | F3 レビュー | F5 リンク |
|---|---|---:|:---:|:---:|:---:|:---:|:---:|
| 01 | 基礎理論・アルゴリズム | 74 | | | | | |
| 02 | プログラミング・言語処理 | 14 | | | | | |
| 03 | コンピュータ構成要素 | 52 | | | | | |
| 04 | システム構成要素 | 44 | | | | | |
| 05 | ソフトウェア・OS | 43 | | | | | |
| 06 | ハードウェア | 15 | | | | | |
| 07 | ユーザーインタフェース・情報メディア | 27 | | | | | |
| 09 | データベース | 63 | | | | | |
| **10** | **ネットワーク** | **104**※2 | ✅2026-08-03 | | | | |
| **11** | **セキュリティ** | **140**※ | ✅2026-08-02 | ✅2026-08-02 | ✅2026-08-02 | ✅2026-08-02 | ✅2026-08-02(189本) |
| 12 | 開発技術 | 87 | | | | | |
| 14 | プロジェクトマネジメント | 44 | | | | | |
| 15 | サービスマネジメント・監査 | 58 | | | | | |
| 17 | システム戦略・企画 | 42 | | | | | |
| 19 | 経営戦略・技術戦略 | 48 | | | | | |
| 21 | ビジネスインダストリ・AI利活用 | 40 | | | | | |
| 22 | 企業活動・会計財務 | 57 | | | | | |
| 23 | 法務・標準化 | 46 | | | | | |
| | **合計** | **998** | | | | | |

記入例：`✅2026-08-02` / `作業中(40/138)` / 空欄＝未着手

> **※2 ネットワークの 74→104 について**
> IPA出題範囲（中分類10の小分類1〜5）と突合し、単語リストに無かった30語を追加した（→ 8章の追加ルール）。
> 初版の教科書は単語リストを骨格にしたため、出題範囲にある項目が抜けていた（→ 2.5章の冒頭）。
>
> **※ セキュリティの +2 について**
> F3レビューでリンク切れ検査を行った結果、単語リストに存在しない語へのリンクが見つかった。
> うち「ファイアウォール」「ワーム」は**既存ページの対比説明が成立するために不可欠**（WAFはFWとの対比が核心、
> トロイの木馬はワームとの対比が核心）だったため、ページを新規作成した（計140ファイル）。
> **`応用情報単語リスト.md` にも追加済み**（966→968語）。以後は上記「単語リストへの追加ルール」に従う。

### 着手順（推奨）

**必ず「教科書 → 用語集」の順で1分野ずつ進める**（→ 1章の大原則）。
下の順序は「どの分野から着手するか」であって、用語集を先行させてよいという意味ではない。

| 順 | 分野 | 理由 | 教科書 |
|---|---|---|:---:|
| 1 | **11 セキュリティ** | 科目B必須・科目Aで最頻出 | ✅完成 |
| 2 | **10 ネットワーク** | 科目B選択候補・分量も多い | ✅完成（2026-08-03） |
| 3 | 09 データベース | 科目B選択候補 | 未着手 |
| 4 | 15 サービスマネジメント・監査 ／ 14 PM ／ 19 経営戦略 | 科目B選択候補（コード不要の文章題） | 未着手 |
| 5 | 残り | | 未着手 |

**現状**：教科書 **2/23**（セキュリティ・ネットワーク）／用語集 **140語**（セキュリティのみ）。
→ **次にやるのは「10 ネットワークの用語集 F1（設計）」**【Opus】。
　ネットワークの教科書が完成したので、1章の大原則を満たしたうえで用語集へ進める状態になった。

---

## 8. 作業手順

### (1) 対象分野の単語を抽出する

> **⚠️ この8章は「用語集の工程」の手順である。教科書を書いている段階では実行しない。**
> 教科書の手順は2.5章にある（基準は出題範囲詳細のみ）。

```bash
cd "/Users/yoshidatomohiro/Library/Mobile Documents/iCloud~md~obsidian/Documents/second_brain/002.Notes/002.Privates/勉強/応用情報技術者試験"

# セクション名は 応用情報単語リスト.md の "# ===== 〜 =====" 行と一致させる
awk '/^# ===== テクノロジ系：セキュリティ/{f=1;next} /^# =====/{f=0} f && /\|/{print}' 応用情報単語リスト.md
```

### (2) 執筆する

- 保存先は **iCloud側**（上記パスの `用語集/<分野フォルダ>/`）
- **Quartz側（Googleドライブ）の `content/` は絶対に編集しない**。
  `sync-content.sh` が rsync `--delete` で上書きするため、書いても消える

### (3) 公開サイトに反映する

```bash
cd "/Users/yoshidatomohiro/Library/CloudStorage/GoogleDrive-t.yoshida.0413@gmail.com/マイドライブ/dev/ap-exam-quartz" && ./sync-content.sh
```

これだけで同期・コミット・push・GitHub Actionsによる自動デプロイまで完了する。
公開URL：https://tyoshida0413-ai.github.io/ap-exam-textbook/

### (4) 記録する

- **この引き継ぎドキュメントの進捗表**（7章）を更新する
- 当日のDaylog `## 作業記録 ✏️` に追記する

---

## 9. 完了前チェックリスト

### 9-0. 教科書の完了前チェック（★これが主目的の判定）

- [ ] `応用情報技術者試験_出題範囲詳細.md` の**該当中分類の小分類1〜Nの知識項目例を、1つ残らず**本文で説明した
      （項目ごとに「どの節で説明したか」を突き合わせる。**これが唯一の完了条件**）
- [ ] **全ての節に番号を振った**（`### N.M`。番号なしの節を章直下に置かない → 罠#5）
- [ ] 想定読者（IT実務経験なし・基本情報レベル未満）の前提を外していない
- [ ] 科目B対策の節・付録A・付録B・付録Cがある
- [ ] **ビルド後のHTMLに生の `**` が残っていない**（→ 罠#19。push前に `npx quartz build` で確認）
- [ ] 進捗表を更新した／`sync-content.sh` で反映した／Daylogに記録した

> **⛔ 教科書の判定に、単語リストとの照合や語数を使わない。**
> 使うのは出題範囲詳細だけ（→ 2.5章冒頭）。

### 9-1. 用語集の完了前チェック

1ファイルごとに確認する。

- [ ] frontmatter に `title` / `分野` / `難度` がある
- [ ] 一言定義（`>` の引用）が1〜2文で書けている
- [ ] 元の単語リストの一行説明の**コピーになっていない**
- [ ] 略語は正式名称と意味が書かれている
- [ ] `[[リンク]]` が最低1本（教科書へのリンクを含む）ある
- [ ] たとえ話は**自然か**（こじつけなら削除する）
- [ ] 1,000〜1,500字に収まっている
- [ ] ファイル名に `/` などの使用不可文字が入っていない

分野を終えたら：

- [ ] **語数の突合**：元の単語リストと作成済みファイルの `title` を `diff` で照合し、差分0を確認した
- [ ] **リンク切れ検査**：下記コマンドで「実体が存在しないリンク」が0件であることを確認した
- [ ] **教科書の節番号が実在するか**確認した（`grep '^#\{2,3\} ' 教科書/*.md` と突合）
- [ ] **ビルド後のHTMLに生の `**` が残っていない**ことを確認した（→ 罠#19。**mdの目視でも自作の正規表現でも検出できない**。push前にローカルビルドで確認すること）
  ```bash
  cd "/Users/yoshidatomohiro/Library/CloudStorage/GoogleDrive-t.yoshida.0413@gmail.com/マイドライブ/dev/ap-exam-quartz"
  npx quartz build
  grep -o '\*\*' public/教科書/<分野>.html | wc -l   # 0 でなければ日本語に挟まれた太字が壊れている
  ```
- [ ] 進捗表を更新した
- [ ] `sync-content.sh` で公開サイトに反映した
- [ ] Daylogに記録した

> **⚠️ リンク切れ検査は必ずこの方法で行う**
> Quartzは未解決のwikilinkに `class="broken"` を**付けない**ため、
> `grep broken` では検出できない（2026-08-02 にこれで8件見逃した）。
> **出力HTMLのhrefを実ファイルと突き合わせる**のが唯一確実な方法：
>
> ```bash
> cd "<Quartzのpublic>"
> grep -rho 'href="[^"]*"' 用語集/<分野>/*.html \
>   | sed 's/href="//;s/"//' | grep -v '^http' | grep -v '^#' \
>   | grep -v '\.css$\|\.js$\|\.xml$\|\.webp$\|\.png$\|\.ico$' \
>   | sed 's|^\.\./\.\./||' | sort -u \
>   | while read -r h; do h="${h%/}"; [ -z "$h" ] && continue; \
>       [ ! -f "$h.html" ] && [ ! -f "$h/index.html" ] && [ ! -d "$h" ] && echo "切れ: $h"; done
> ```
>
> 検出された語は次のいずれかで処理する：
> (1) 単語リストに**ある** → 後で作られるので放置してよい
> (2) 単語リストに**ない**が対比の要・頻出 → **ページを新規作成し、単語リストにも追加する**（→ 下記ルール）
> (3) 単語リストに**なく**説明も他ページで足りる → **ブラケットを外して平文にする**

### ★ 単語リストへの追加ルール（2026-08-02 決定・許可不要）

作業中に「**元の `応用情報単語リスト.md` に無いが、試験に必要と判断した語**」が出てきたら、
**ユーザーの許可を取らずに、その場で単語リストへ追加してよい**。

- 追加先：`応用情報単語リスト.md` の該当分野セクション。**対比する語の隣**に置く
  （例：ファイアウォールはWAFの直前、ワームはトロイの木馬の直前）
- 形式：`用語 | 説明`（1行1語。説明は1〜2文）
- **禁止事項**：説明文に `[1]` のような角括弧＋数字を書かない（朝のルーティンが参照番号として自動削除する）
- 追加後は必ず検証する：
  ```bash
  # 総語数・分野別語数・重複チェック
  grep -c "^[^#[:space:]].*|" 応用情報単語リスト.md
  awk -F'|' '/^[^#[:space:]].*\|/{gsub(/ +$/,"",$1); print $1}' 応用情報単語リスト.md | sort | uniq -d
  ```
- このファイルは**毎朝のDaylogルーティンが参照**している（ランダム出題の母集団）。
  追加は安全（母集団が増えるだけ）だが、**削除・大幅な書き換えは影響が出る**ので行わない

**追加実績**：
- 2026-08-02 ファイアウォール・ワーム（966→**968語**、セキュリティ138→**140語**）
- 2026-08-03 ネットワーク30語（968→**998語**、ネットワーク74→**104語**）。
  IPA出題範囲との突合で判明した未収録語（LAN／WAN／イントラネット／エクストラネット／センサーネットワーク／
  全二重と半二重／調歩同期／多重化／誤り制御／ARQ／HDLC／モデム／DSU／ONU／FTTH／単一障害点／STP／
  ブロードキャストストーム／リンクアグリゲーション／VRRP／専用線／広域イーサネット／IP-VPN／
  ネットワークOS／トラフィック監視／IEEE 802.11／LTE／HTTP/2／HTTP/3／CORBA）。
  **WPA3とロードバランサは他分野に既出だったため追加せず**（`uniq -d` で検出して取り下げた）

---

## 10. 決定事項の記録（変更する場合はここを更新する）

| 項目 | 決定 | 決定日 |
|---|---|---|
| **⛔ 教科書の基準** | **IPA出題範囲（`応用情報技術者試験_出題範囲詳細.md`）のみ。教科書の工程では単語リストを開かない。完了判定も出題範囲の項目突合だけで行う**（→ 2.5章冒頭） | 2026-08-03 |
| **⛔ 単語リストの位置づけ** | **用語集専用の補助資料**。参照してよいのは用語集の工程（F1〜F5）に入ってから | 2026-08-03 |
| **⛔ 制作順序** | **教科書が先、用語集が後。順序は動かさない**（用語集先行はユーザーの明示指示時のみ） | 2026-08-02 |
| **主従関係** | **教科書がメイン。用語集はそれを拡張するために存在する** | 2026-08-02 |
| **教科書のモデル** | **Opus**（手本を作る側であり、章設計とたとえ話の創出が価値の中核のため。→ 2.1） | 2026-08-02 |
| 用語集のモデル | F1設計＝Opus → F2量産＝Sonnet → F3レビュー＝Opus（全件レビューはしない） | 2026-08-02 |
| 想定読者 | 情報系大学卒だがIT実務経験なし・基本情報レベル未満 | 2026-08-02 |
| 用語集の対象 | 単語リストの全語。分野単位で順次 | 2026-08-02 |
| 用語集1語の分量 | 1,000〜1,500字 | 2026-08-02 |
| 用語集の構成 | 分野ごとのサブフォルダ（教科書の中分類番号に合わせる） | 2026-08-02 |
| 単語リストへの追加 | **許可不要**。必要と判断した語はその場で追加してよい（→ 8章） | 2026-08-02 |
| **F5 リンクの粒度** | **段落ごと初出1回＋表の中も対象**（セキュリティで164本）。除外は見出し・コードブロック・付録のみ（→ 2.6） | 2026-08-02 |
| **F5 のモデル** | **Sonnet**（リンク先が確定済みで機械的・検証可能なため。→ 2.7） | 2026-08-02 |
| **F5 のリンクターゲット** | **必ず実ファイル名を主キーにする**（title/aliasの表記のまま使わない）。理由は罠#8（→ 2.6） | 2026-08-02 |
| 表記ゆれ | **同一視する**。ファイルは増やさず `aliases` で吸収（ディジタル署名＝デジタル署名） | 2026-08-02 |
| **本文幅（TODO-1）** | **画面幅に応じて可変・上限1400px**。**2026-08-03 実装・CDP検証済み**（真因は当初の推測と異なった → 2.8章） | 2026-08-02（実装2026-08-03） |
| **Stacked Pages（TODO-2）** | **🛑 保留中**。有効化してpush済みだが、実装が本来の要望（元ページを残して並べて表示）を満たしていないと判明。対応方針はユーザー確認中（→ 2.8章の訂正部分） | 2026-08-02（実装2026-08-03、問題判明も2026-08-03） |
| **TODO-1/2のpush** | 2026-08-03 push・デプロイ済み。TODO-1は問題なし。**TODO-2は要望未達成のまま公開中**、対応（無効化に戻す等）はユーザー指示待ち | 2026-08-03 |
| 着手順 | セキュリティから。次はネットワーク（**教科書は2026-08-03に完成**。以降は同分野の用語集→F5） | 2026-08-02 |
| **教科書の分量** | 目安超過時は**付録から削る。因果説明は削らない**（ネットワークで46,500字になった実績 → 2.5章） | 2026-08-03 |
| 引き継ぎの範囲 | このファイルは**フォルダ全体**の引き継ぎ（用語集専用ではない） | 2026-08-02 |

---

## 更新履歴

| 日付 | 版 | 内容 |
|---|---|---|
| 2026-08-03 | 3.0.0 | **プロジェクトの主目的と、資料の主従関係を厳密化**（ユーザー指示）。目的は「**試験範囲を網羅して合格すること**」であり、**教科書がその主目的そのもの**、用語集と単語リストはその補助であると冒頭に明記。**教科書の工程では `応用情報単語リスト.md` を一切参照しない**（骨格にも完了判定にも使わない）というルールを、冒頭・「絶対に守る6つのルール」・2.5章冒頭・1章の工程図・8章・9章・10章の決定事項に反映。9章に**教科書専用の完了前チェック（9-0）を新設**し、判定を「出題範囲詳細の知識項目例を1つ残らず説明したか」だけに限定。進捗表の「語数」列は用語集の指標であって教科書の完成度とは無関係である旨を明記 |
| 2026-08-03 | 2.9.0 | **🛑 教科書の作り方の欠陥を修正。** ネットワーク教科書の初版は**単語リストを骨格にして組み立てており、IPA出題範囲にある項目が抜けていた**（ユーザー指摘で発覚）。`応用情報技術者試験_出題範囲詳細.md` の中分類10・小分類1〜5と突合し、抜けていた項目を教科書1.1.0で補完（LAN/WAN、イントラネット・エクストラネット、伝送方式と回線、多重化、誤り制御・ARQ、HDLC、回線接続装置、単一障害点・STP・リンクアグリゲーション・VRRP、HTTP/2・HTTP/3、CORBA、通信サービス、運用管理、ロードバランサ、IEEE 802.11規格・WPA3、LTE、センサーネットワーク）。**2.5章の冒頭に「章立ての起点はIPA出題範囲であって単語リストではない」という手順を新設**し、検証も2本立て（単語リスト照合＋出題範囲の項目突合）に変更。単語リストへ30語を追加（968→998語、ネットワーク74→104語） |
| 2026-08-03 | 2.8.0 | **教科書2本目『10_ネットワーク』を作成（約46,500字・0〜8章＋付録A/B/C）**。単語リストのネットワーク**74語すべてを本文に配置**し、機械照合で未出現0を確認。**全ての節に番号を振り**（罠#5の回避）、用語集からは「N.M 節」で参照できる。F5前なのでwikilinkは0本（意図どおり）。**初回デプロイ後の本番HTML実測で、日本語に挟まれた太字14箇所が生の `**` のまま表示されていることを発見・修正して再デプロイ**（→ 罠#19。閉じる `**` の直前が全角括弧だと太字にならない。**mdの目視では検出できないため、出力HTMLで `grep -o '\*\*'` が0件であることを必ず確認する**）。**2.5章に分量超過の実績と、超過時は付録から削り因果説明は削らないという方針を追記**。進捗表・着手順・決定事項を更新 |
| 2026-08-03 | 2.7.0 | **TODO-2を自作の用語サイドパネルとして実装し直し、CDP実測で全23項目合格**（ローカルのみ・未push）。stacked-pagesは無効化。設計と実装結果を**2.9章**に新設（2.8章は失敗の記録として保持）。実装中に踏んだ罠を#15〜#18として追加：(15)`componentResources.css` に渡したCSSは `@layer quartz-base` 内かつ base.scss より前に置かれレイアウト指定が効かない→custom.scssから`@use`する、(16)SPAルータは`defaultPrevented`を見ないため`preventDefault`だけでは遷移を止められない→`stopPropagation`が必要、(17)`.center`は自動配置のグリッドアイテムでサイドバーを`display:none`にすると本文が48pxに潰れる→`visibility:hidden`＋幅0にする、(18)`scroll-behavior:smooth`のためCDPでのスクロール計測が誤検知する |
| 2026-08-03 | 2.6.1 | **🛑 訂正：TODO-2（Stacked Pages）は「完了」ではなかった。** push・デプロイ後にユーザーが実際のブラウザで確認したところ見た目が破綻しており、調査の結果、(1) 有効なドキュメント（`docs/plugins/StackedPages.md`）の説明「元ページを残して並べて表示」自体が実装（通常のページ遷移＋タブ帯のみ）と一致しておらず、**本来の要望を満たしていなかった**、(2) 2ページ遷移のみのCDP検証では見つからなかった「Home→用語集→11_セキュリティ→機密性」の4段階遷移で、タブ積み上げによる紛らわしい表示（サイト名の二重表示）が発生することが判明。「検証済み」としていた記述を訂正し、2.8章に訂正セクションを追加。**対応（無効化に戻す等）はユーザー指示待ちで、次の担当は勝手に実装を進めないこと** |
| 2026-08-03 | 2.6.0 | **TODO-1（本文幅）・TODO-2（Stacked Pages）を実装し、CDP実測で検証**（Quartzリポジトリのローカル変更のみ・push未実施）。2026-08-02の原因分析が誤りだったと判明：真因は `@quartz-themes/core` が注入するid込み詳細度のunlayered CSS。`custom.scss` セクション1を全面書き換え（`div#quartz-root.page div#quartz-body` を詳細度で上書き）、`quartz.config.yaml` で `stacked-pages` を有効化。有効化直後は左サイドバーとテキストが重なるバグを発見し、`custom.scss` セクション4で修正。2.8章を全面更新、罠#13・#14を追加 |
| 2026-08-02 | 1.0.0 | 初版作成（用語集専用として） |
| 2026-08-02 | 2.1.0 | **F5（教科書→用語集のリンク設定）を新設**。2.6章にルール（段落ごと初出＋表も対象・除外場所・表記ゆれ・参考実装・検証手順）、2.7章にモデル判断（Sonnet）を追加。1章の工程を5段階に、進捗表に「教科書」「F5リンク」列を追加 |
| 2026-08-02 | 2.5.0 | **未着手TODO 2件を2.8章として記録**（実装はしていない）。(1)高解像度モニターで本文が広がらない問題（3440pxでも本文930px＝27%。原因は`#quartz-body`のグリッドが1500pxで頭打ち）＝方針「画面幅可変・上限1400px」で決定済み。(2)用語クリックでサイドパネルに表示＝**Quartz標準の`stacked-pages`プラグインで実現可能**と判明（現在`enabled:false`・本体未インストール）。**両方ともユーザーから「翌日こちらから指示する」との指示があり着手しない**。3分ブリーフィングにもTODO表を追加 |
| 2026-08-02 | 2.4.0 | **表の配色を作り直した**。2.3.0で黄色を消す際に薄くしすぎ、ダークで**ヘッダrgb(40,40,40)と偶数行rgb(35,35,35)の差がわずか5**となり見分けがつかなくなっていた（ユーザー指摘）。加えて**行の横罫線が0pxで消えていた**。→ `--tbl-head/zebra/line/line-strong` をライト・ダーク別に定義し、**横罫線で行を区切る**方式へ変更。実測でヘッダと偶数行の差をダーク22／ライト12に確保。罠#12を追加 |
| 2026-08-02 | 2.3.0 | **F5後にマークダウン破壊とサイト表示の問題を発見・修正**。(a)表内の`[[a\|b]]`の`\|`がセル区切りと解釈され表が崩壊（12箇所）、(b)`**[[語]]**`等でCJKの`**`がliteral化（計160箇所超）→ 2.6章に**検証(6) F5後マークダウン検証**と壊れる3パターンの表を新設。(c)表のゼブラに使った`var(--highlight)`の実体が**黄色**（`@quartz-themes/core`がconfigを上書き）→中立グレーへ変更、リンクの黄色チップも淡色化。(d)バックリンク141件が目次を圧迫（目次287px）→グラフとバックリンクに上限を設け**目次602pxへ拡大**。罠#9〜#11を追加 |
| 2026-08-02 | 2.2.0 | **セキュリティのF5を実行し完了（189本）。実行中に重大な罠#8を発見・修正**：`[[表記そのまま]]`でリンクすると、aliasの場合に`alias-redirects`が生成する**空のリダイレクトページ**を指し、クリックは通るがホバープレビューが機能しなくなる（href実体突合では検出不可）。titleに`/`を含む語（ISO/IEC 15408等）では罠#4のスラッシュ問題も再発した。対処：**リンクターゲットを実ファイル名（basename）に統一**し、表記は`\|`の後ろに保持する方式に修正。参考実装スクリプトと検証手順(5)をこの方式で全面更新。当初202本→修正後189本 |
| 2026-08-02 | 2.0.0 | **フォルダ全体の引き継ぎに拡張**（`00_引き継ぎ_用語集.md` → `00_引き継ぎ.md` にリネーム）。「セッション開始時の3分ブリーフィング」「0章 フォルダの全体像」「2.1 教科書のモデル選択」「2.5 教科書の作成基準」を新設。大原則「教科書が先、用語集が後」を明文化 |
