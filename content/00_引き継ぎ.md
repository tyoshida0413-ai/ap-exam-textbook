---
title: 応用情報技術者試験 引き継ぎ
version: 2.3.0
created: 2026-08-02
updated: 2026-08-02
対象: このフォルダ（応用情報技術者試験）全体
---

# 応用情報技術者試験フォルダ 引き継ぎドキュメント

> **このファイルの役割**
> このフォルダの作業を、セッションをまたいで・モデルをまたいで同じ品質で継続するための
> **唯一の基準書**。**作業を始める前に必ず全文を読むこと。**
> 方針・書式・分担・進捗はすべてここで管理する。

---

# 🚀 セッション開始時の3分ブリーフィング

**セッションをクリアした直後でも、ここだけ読めば前回と同じ理解度で再開できる。**

## 現在地（2026-08-02 時点）

| 成果物 | 進捗 | 次の一手 |
|---|---|---|
| **教科書**（メイン・全23分野） | **1/23** 完成（11_セキュリティ） | **10 ネットワークの教科書を書く** ← これが次【Opus】 |
| **用語集**（拡張・分野ごと） | **140語** 完成（11_セキュリティ） | 教科書ができてから着手 |
| **F5 リンク設定**（教科書→用語集） | ✅**完了**（セキュリティ・189本） | ネットワークのF5は教科書・用語集完成後 |
| 公開サイト | 稼働中・全ページ到達確認済み | ─ |

> ✅ セキュリティは相互リンクが完成済み（教科書→用語集189本／用語集→教科書140本）。
> ただし**189本はホバー実測で罠を踏んで修正した後の数**（当初202本→バグ発覚→189本に修正）。
> 次分野で同じスクリプトを使う際は、**必ず2.6章の「実ファイル名を主キーにする」実装を使うこと**（罠#8）。

## 絶対に守る5つのルール

1. **⛔ 教科書が先、用語集が後。** 順序は動かさない。用語集を先に作るのはユーザーの明示指示があるときだけ
2. **想定読者は「情報系大学卒だがIT実務経験なし・基本情報レベル未満」。** たとえ話と「なぜ」を必ず書く
3. **編集は必ずiCloud側（このフォルダ）で行う。** Quartz側の `content/` は上書きされるので直接編集しない
4. **単語リストに無い必要な語は、許可を取らず追加してよい**（→ 8章の追加ルール）
5. **モデル分担**：教科書執筆と用語集F1設計・F3レビューはOpus、用語集F2量産はSonnet（→ 2章）

## 既知の罠（過去に実際にやらかしたこと・繰り返さない）

| # | 罠 | 対処 |
|---|---|---|
| 1 | **想定読者を間違えて教科書を書いた** | 執筆前に必ず読者前提を確認。セキュリティは全面書き直しになった |
| 2 | **`sync-content.sh` を忘れて古い状態でビルド**し「反映された」と誤認 | ビルド前に `content/` の中身を確認する |
| 3 | **`grep broken` でリンク切れ0件と誤報告**（Quartzはbrokenクラスを付けない） | 9章の**href実体突合コマンド**を使う（唯一確実） |
| 4 | **`[[SSL/TLS]]` が `ssl/tls` を指してリンク切れ**（`/`がパス区切り扱い） | `[[SSL-TLS\|SSL/TLS]]` 形式で書く（→ 3章） |
| 5 | **教科書の存在しない節番号を13ファイルで参照** | 教科書は**全節に番号を振る**（→ 2.5章）。参照前に見出しをgrepで照合 |
| 6 | **CSSのカスタムがプラグインCSSより先に読まれて効かない** | `body` を前置して詳細度を上げる |
| 7 | **語数の数え間違い**（行数と語数の取り違え／1語漏れ） | 完了前に元リストと `diff` で突合（→ 9章） |
| 8 | **F5でaliasリンクの中身が空になる**（`[[デジタル署名]]`と書くと空のリダイレクトページを指し、ホバー時に何も表示されない。href実体突合では検出できない） | リンクターゲットは常に**実ファイル名**にし、表記は `\|` の後ろに書く（→ 2.6章） |
| 9 | **F5がマークダウンを壊す**（表の中の `[[a\|b]]` は `\|` がセル区切りと解釈され表が崩壊／`**[[語]]**` はCJKで `**` がliteral化） | 2.6章の**F5後マークダウン検証**を必ず行う。表内は `\|` をエスケープ、太字の境界にリンクを置かない |
| 10 | **`var(--highlight)` は黄色**（configの色設定は `@quartz-themes/core` に上書きされており、実体は `rgba(255,208,0,0.4)`） | 表のゼブラ等に `--highlight` を使わない。中立的な `rgba(128,128,128,0.07)` を使う |
| 11 | **CSSを直してもCDP実測で変化なし** | ブラウザキャッシュ。`Network.setCacheDisabled` を有効にしてから測る |

## 最初に実行するコマンド

```bash
cd "/Users/yoshidatomohiro/Library/Mobile Documents/iCloud~md~obsidian/Documents/second_brain/002.Notes/002.Privates/勉強/応用情報技術者試験"
ls 教科書/ 用語集/           # 何が完成しているか確認
```

---

## 0. このフォルダの全体像

**目的**：応用情報技術者試験（令和8年度）の合格。現行APは令和8年度で終了するため受験機会は前期・後期の2回のみ。

### ファイル構成

```
応用情報技術者試験/
├─ 00_引き継ぎ.md              ← このファイル（唯一の基準書）
├─ index.md                    ← 公開サイトのトップページ（目次）
├─ 応用情報技術者試験全体像.md    ← 試験制度・学習戦略（調査済み・随時追記）
├─ 応用情報技術者試験_出題範囲詳細.md ← IPA出題範囲の詳細（抜け漏れチェック用）
├─ 応用情報単語リスト.md         ← 968語。毎朝のDaylogルーティンが参照する
├─ プロフェッショナルデジタルスキル試験_調査.md ← 令和9年度以降の新試験の調査
├─ 教科書/                      ← ★メイン成果物（IPA中分類ごと・全23本）
└─ 用語集/                      ← ★教科書の拡張（1単語1ファイル）
```

### 公開サイト（Quartz + GitHub Pages）

- URL：https://tyoshida0413-ai.github.io/ap-exam-textbook/
- Quartz本体：`~/Library/CloudStorage/GoogleDrive-.../マイドライブ/dev/ap-exam-quartz/`
  （**iCloud外に置くのは意図的**。node_modules等の大量ファイルがiCloud同期と干渉するため）
- 反映：`cd <上記> && ./sync-content.sh` の1コマンドのみ
- **編集は必ずiCloud側（このフォルダ）で行う。** Quartz側の `content/` は rsync `--delete` で
  上書きされるコピーなので、直接編集しても消える

### 全体の進捗

| 成果物 | 進捗 |
|---|---|
| **教科書** | **1/23**（11_セキュリティのみ完成） |
| **用語集** | **140語**（11_セキュリティのみ完成） |

**→ 次にやるのは「10 ネットワークの教科書」**（→ 1章の大原則）

---

## 1. 2つの成果物と、その関係

このフォルダの成果物は2つ。**教科書がメインで、用語集はその拡張**という関係にある。

- **教科書**：IPA中分類ごとに1本（全23本）。通読して**体系を理解する**ためのもの
- **用語集**：1単語につき1ファイル。**分からない語を引く**ためのもの

> # ⛔ 大原則：教科書が先、用語集が後（順序は動かさない）
>
> - **教科書がメイン。用語集はそれを拡張するために存在する。**
> - **その分野の教科書が完成していない状態で、用語集を先に作ってはならない。**
>   （ユーザーから明示的な指示があった場合のみ例外）
> - 理由：用語集の各ファイルは「教科書：`[[分野名]]` の N 節」で**教科書へ必ずリンクする**設計になっている。
>   教科書が無ければこのリンクが張れず、**辞書から体系へ戻る導線が失われる**。
>   セキュリティで機能したのは、教科書が先にあったからこの相互リンクが成立したため。
>
> **1分野の正しい進め方（全5工程）**：
> ```
> (1) 教科書/<分野>.md を作る       ← ここが起点。これ抜きで(2)へ進まない【Opus】
>          ↓
> (2) 用語集のF1（設計：難語選定・混同ペア表・見本3〜5本）【Opus】
>          ↓
> (3) 用語集のF2（量産：残り全語）【Sonnet】
>          ↓
> (4) 用語集のF3（難語だけレビュー）【Opus】
>          ↓
> (5) 【F5】教科書→用語集のリンク設定【Sonnet】 → 2.6章
>          ↑ ここまでやって初めて「相互に行き来できる網」が完成する。
>            (4)で終わらせるとリンクが片道のままになる
> ```

### 教科書との役割分担

| | 教科書（`教科書/`）＝**メイン** | 用語集（`用語集/`）＝**拡張** |
|---|---|---|
| 単位 | IPA中分類ごと（全23本） | **1単語ごと** |
| 読み方 | 通読して体系を理解する | **分からない語を引く** |
| 分量 | 1本 15,000〜35,000字 | 1語 **1,000〜1,500字** |
| 役割 | 「なぜそうなるか」の筋道 | その語の**単体での完結した理解** |
| 作る順 | **先** | **後**（教科書に依存する） |

### なぜ1ファイル1単語なのか

Obsidian／Quartz上で `[[SQLインジェクション]]` とリンクすると、
**ホバーで内容がポップアップ表示**され、逆に単語側からは「どの教科書で使われているか」が
**バックリンク**で分かる。孤立した用語集ではなく、**教科書と相互に行き来できる網**にすることが目的。

---

## 2. モデルの分担（重要）

### 全体の割り当て

| 作業 | モデル | 理由 |
|---|---|---|
| **教科書の執筆** | **Opus** | ↓ 2.1で詳述 |
| 用語集 F1（設計） | **Opus** | 難語選定・混同ペアの見極めは横断的判断が要る |
| 用語集 F2（量産） | **Sonnet** | 見本と一行定義がある定型的なコンテンツ生成 |
| 用語集 F3（レビュー） | **Opus** | 難語だけ点検（全件レビューはしない） |

### 2.1 なぜ教科書はOpusなのか（2026-08-02 実証にもとづく判断）

**用語集F2でSonnetが成功した理由**を裏返すと、教科書との差がはっきりする。

| | 用語集F2（Sonnetで成功） | 教科書 |
|---|---|---|
| 手本 | 見本4本＋教科書があった | **無い**（自分が手本を作る側） |
| 元ネタ | 一行定義が既にある | **無い**（分野の体系から組む） |
| 作業の性質 | 定義を膨らませる定型作業 | **章立ての設計＋たとえ話の創出** |
| 分量 | 1語 1,000〜1,500字 | **1本 15,000〜37,000字を一貫した品質で** |

**Sonnetの品質は実際に高い**（F3で難語42語をレビューし、技術的誤りはゼロだった。
MACの「夫婦共有の合言葉スタンプ」のように、教科書に無いたとえ話も自力で作れている）。

それでも教科書をOpusにするのは、**教科書が「手本を作る側」だから**。
用語集F2が成立したのは教科書という基準があったからで、
**その基準自体の品質が落ちると全体が連鎖的に劣化する**。
想定される失敗も「事実誤り」ではなく「**説明が平板になる**」ことであり、
これは想定読者（IT実務経験なし）にとって致命的になる。

> **ただしOpusも完璧ではない。**
> 「教科書の節に番号が無く、用語集13ファイルが存在しない節を参照した」という構造的欠陥は、
> Opusが書いた教科書に起因していた。**モデルに関わらず9章の検証は必須。**

**例外**：付録A（用語チェックリスト）と付録C（NotebookLMプロンプト集）は定型作業のため
Sonnetでも足りる。ただし分割の手間が節約効果を上回るなら、教科書1本を通してOpusで書いてよい。

### 2.2 用語集の3フェーズ

品質差が出るのは一部の用語だけなので、**Opusを使う場面を限定する**。

```
【フェーズ1】Opus … 分野ごとの「設計」
  ├ 難語リストの選定（その分野で品質が効く語を抽出）
  ├ 混同ペア表の作成（どの語とどの語が紛らわしいか）
  └ 難語の見本を3〜5本執筆（基準を示す）
        ↓
【フェーズ2】Sonnet … 量産
  └ 見本に沿って、その分野の残り全語を執筆
        ↓
【フェーズ3】Opus … 限定レビュー
  └ フェーズ1で挙げた「難語」だけを点検する（全件レビューはしない）
```

### なぜこの分担なのか

- 全語の大半は「一行定義を膨らませれば済む」**定型的なコンテンツ生成**。
  グローバルCLAUDE.mdの基準どおり、ここに上位モデルは不要。
- 一方、**たとえ話の質**と**混同ペアの見極め**は横断的な判断が要るためOpusが有効。
- **「全部Sonnetで作ってから全部Opusでレビュー」はしない。**
  数百ファイルを流し読みするレビューは、実質的に追認に終わり、手直しが必要な箇所を
  見落としたまま「レビュー済み」の体裁だけが残るため。

### モデル切り替えのタイミング

Claude側からモデルは変更できない。**フェーズが変わったら、ユーザーに `/model` での切り替えを依頼する。**

---

## 2.5 教科書の作成基準（★メイン成果物）

**唯一の完成例：`教科書/11_セキュリティ.md`（約36,800字）。新しい分野を書くときは必ずこれを読んで踏襲する。**

### 想定読者（全分野で共通・絶対に外さない）

> **情報系の大学は出ているが、IT実務経験はなく、基本情報レベル未満。**

この前提を外すと教科書が成立しない。セキュリティでは、当初この前提を誤って書き始めたため
**全面書き直し（v1.0.0→v2.0.0）**になった。最初に必ず確認すること。

### 各節の構成（この順で書く）

```
(1) 【まず日常の話】   身近な場面でイメージをつかむ
(2) 【ITだとこうなる】 そのイメージがIT用語のどこに対応するか
(3) 【用語まとめ】     試験に出る形での整理（表）
(4) 【試験ではこう出る】ひっかけ・頻出パターン
```

### 執筆ルール

- **たとえ話を必ず先に置く**（例：公開鍵暗号＝「閉める鍵と開ける鍵が別々の錠前」）
- **略語は正式名称とその意味を書く**（IDS＝Intrusion **Detection** System＝侵入「検知」）
- **「なぜ」を必ず書く**（ログを別サーバへ送る → なぜ？＝管理者自身に消させないため）
- **誤解パターンを明示する**（✕「署名すれば中身も守られる」→ ○「実印を押しても文面は読める」）
- **計算は途中式まで**（鍵の数 100×99÷2＝4,950）
- 丸囲み数字（①②③）は使わない。`(1)(2)(3)` を使う
- **こじつけたたとえ話は書かない**（制度名・組織名など自然な比喩がない語では省略する）

### 必須の構成要素

| 要素 | 内容 |
|---|---|
| 本文 | 分野の体系を通読で理解できる筋道 |
| **科目B対策の節** | 設問パターン・解答の型・そのまま使える解答ストック |
| **付録A 用語チェックリスト** | 想起用（声に出して説明できるか試す） |
| **付録B 想定問題** | 科目A形式（四択10問）＋科目B形式（記述式）。**解答は `<details>` で折りたたむ** |
| **付録C NotebookLM深掘りプロンプト集** | 理解確認／具体例／問題生成／弱点掘り下げ／分野横断の5カテゴリ |

### 分量の目安

| 分野の性質 | 目安 |
|---|---|
| 科目B必須・最頻出（セキュリティ） | 30,000〜37,000字 |
| 科目B選択候補（ネットワーク、DB、PM、監査ほか） | 20,000〜30,000字 |
| 科目A中心 | 15,000〜20,000字 |

### 章立てのルール（用語集からのリンクに直結する）

- **章は `## N. タイトル`、節は `### N.M タイトル` の形式にする**
- **番号なしの小見出し（`### 【まず日常の話】` など）を章直下に置くと、
  用語集から「N.M 節」で参照できなくなる。**
  セキュリティの0章・1章がこの形になっており、**用語集13ファイルで存在しない節を参照する不具合**が発生した。
  **新しい分野では、全ての節に番号を振ること。**

---

## 2.6 教科書→用語集のリンク設定【F5】（用語集の完成後に行う）

### なぜやるのか

用語集の各ファイルは教科書へリンクしている（用語集→教科書：140本）。
しかし**教科書から用語集へのリンクは、作った直後は0本**である。
理由は制作順で、教科書を書いた時点では用語集がまだ存在しないため。

このままだと**片道**になり、「教科書を読んでいて分からない語があっても用語集へ飛べない」。
1章に掲げた「**教科書と相互に行き来できる網**」が成立しないので、
**用語集がその分野で完成したら、必ずこの作業を行う**。

### 粒度のルール（2026-08-02 実測にもとづき決定）

> ## ✅ **段落ごとに初出1回だけリンクする。表の中も対象にする。**

セキュリティ（36,800字・用語140語）での実測値：

| 粒度 | リンク数 | 判定 |
|---|---:|---|
| 節ごと初出（表を除く） | 58 | 少なすぎる。辞書として機能しない |
| 段落ごと初出（表を除く） | 71 | まだ少ない |
| 節ごと初出（表を含む） | 124 | ─ |
| **段落ごと初出（表を含む）** | **164** | **← これを採用** |
| 全出現をリンク | 352 | 多すぎる。本文がリンクだらけで読めない |

**「段落」の定義**：空行で区切られたかたまり。同じ段落内に同じ語が2回出たら、**最初の1回だけ**リンクする。

### ⚠️ 表の中も必ずリンクする（当初の判断を実測で覆した経緯）

当初は「表の中はリンクしない」と決めかけたが、**これは誤り**だったので撤回した。

- 教科書は**比較表が主役**の構成で、用語の出現352回のうち**97回が表の中**にあった
- 表を除外すると、`IDS`↔`IPS`、`CVE`↔`CWE`↔`CVSS` のような
  **最も引きたくなる対比表からリンクが消える**。辞書として使えなくなる
- 除外しようとした理由は「レイアウト崩れの懸念」だったが、
  **用語集の混同ペア表では既に表内リンクを使っており、公開サイトで正常表示されている**。懸念は成り立たない

### リンクしない場所（除外）

| 場所 | 理由 |
|---|---|
| **見出し行**（`#` で始まる行） | 見出しがリンクだらけになると構造が読めない |
| **コードブロック**（``` で囲まれた範囲） | 図解・ASCIIアートが崩れる |
| **付録すべて**（`## 付録A` 以降） | 特に**付録A 用語チェックリスト**は想起用。リンクがあると答えが見えてしまう。付録B想定問題・付録Cプロンプト集も対象外 |

セキュリティでは、付録に130回の出現があったが、これらはすべて除外した。

### 表記ゆれの扱い

> **厳密にしすぎない。同じものは同じ語として扱い、用語集のファイルは増やさない。**

- 例：**ディジタル署名／デジタル署名** は同一。ファイルは1つだけ作る
- 吸収の方法：用語集ファイルの frontmatter `aliases:` に別表記を書く
  ```yaml
  ---
  title: ディジタル署名
  aliases:
    - デジタル署名
  ---
  ```
- リンクを張るときは `[[ディジタル署名]]` に統一してよい。
  本文の表記を保ちたい場合は `[[ディジタル署名|デジタル署名]]` を使う
- **表記ゆれのたびに新しいファイルを作らない**（語数が無意味に増え、内容も重複する）

### ⚠️ リンクターゲットは必ず「実ファイル名」にする（2026-08-02 実測で発覚した重大な罠）

**当初の実装は失敗した。** `[[表記そのまま]]`で置換すると、次の2つが同時に壊れる。

1. **aliasでリンクすると、ホバープレビューの中身が空になる**
   `[[デジタル署名]]`（実ファイルは`ディジタル署名.md`、`デジタル署名`はalias）と書くと、
   Quartzは`alias-redirects`プラグインにより**`meta http-equiv="refresh"`だけの空リダイレクトページ**を
   ルート直下に自動生成し、リンクはそこを指す。**クリックすれば最終的に正しいページへ着くので
   リンク切れ検査（href実体突合）ではすり抜けてしまう**が、ホバー時にfetchされる中身は
   `<meta http-equiv="refresh">`のみで、辞書の核心である**ホバープレビューが機能しない**。
   （CDPで実測：`article.popover-hint`が存在せず、ポップオーバーに何も表示されないことを確認済み）

2. **titleに`/`を含む語（ISO/IEC 15408、JPCERT/CC、S/MIME）で、3章と同じスラッシュ問題が再発する**
   `/`が実ファイル名では`-`に置換されている（`ISO-IEC 15408.md`）のに、
   titleの表記のまま`[[ISO/IEC 15408]]`と書くと`iso/iec-15408`という存在しないパスを指す。

**正しい実装**：リンクターゲットは常に**用語集ファイルのbasename（拡張子を除いたファイル名）**にし、
表記がbasenameと異なる場合（alias・別表記）だけ`[[basename|見つかった表記]]`にする。

### 実行手順

**担当モデル：Sonnet**（→ 2.7）

1. 対象分野の用語一覧を、**「ファイル名(basename)」→「title・aliasesの全表記」**の形で取得する
   （表記だけを集めてリンクターゲットにしてはいけない。ファイル名を主キーにする）
2. **長い語から順に**処理する（`公開鍵暗号方式` を先に処理しないと `公開鍵` が誤爆する）
3. 上記ルールで `[[basename]]` または `[[basename|表記]]` に置換する
4. **検証**（→ 下記4項目。特に(4)は必須）

参考実装（実行後は必ず4の検証を行うこと。ファイル名を主キーにしている点が最重要）：

```bash
cd "/Users/yoshidatomohiro/Library/Mobile Documents/iCloud~md~obsidian/Documents/second_brain/002.Notes/002.Privates/勉強/応用情報技術者試験"
python3 - <<'PY'
import re, glob, os
FIELD = "11_セキュリティ"          # ← 対象分野に変える
BOOK  = f"教科書/{FIELD}.md"

# term_to_slug: 表記(title自身 or alias) -> 実ファイル名(=正しいリンクターゲット)
term_to_slug = {}
for f in glob.glob(f"用語集/{FIELD}/*.md"):
    base = os.path.splitext(os.path.basename(f))[0]   # ★ 実ファイル名を主キーにする
    if base.startswith("00_"): continue
    fm = open(f, encoding='utf-8').read().split('---')[1]
    title=None; aliases=[]; in_aliases=False
    for line in fm.split('\n'):
        if line.startswith('title:'): title=line[6:].strip()
        elif line.startswith('aliases:'): in_aliases=True
        elif line.startswith('  - ') and in_aliases: aliases.append(line[4:].strip())
        elif line and not line.startswith(' '): in_aliases=False
    if title:
        term_to_slug[title] = base
        for a in aliases: term_to_slug[a] = base

terms = sorted(term_to_slug.keys(), key=len, reverse=True)   # 長い語から（誤爆防止）

src = open(BOOK, encoding='utf-8').read().split('\n')
appendix = next((i for i, l in enumerate(src) if l.startswith('## 付録')), len(src))

out=[]; infence=False; para=0; prev_blank=True; seen=set()
for i, l in enumerate(src):
    if l.strip().startswith('```'): infence=not infence; out.append(l); continue
    if l.strip()=='': prev_blank=True; out.append(l); continue
    if prev_blank: para+=1
    prev_blank=False
    if i>=appendix or infence or l.startswith('#'):
        out.append(l); continue            # 付録・コード・見出しは除外（表は対象）
    for t in terms:
        slug = term_to_slug[t]
        k=(slug, para)                      # ★ 実ファイル名単位で重複判定（表記違いでも同一ページなら1回）
        if k in seen: continue
        parts=re.split(r'(\[\[[^\]]*\]\])', l)
        done=False
        for j,p in enumerate(parts):
            if p.startswith('[['): continue
            if t in p:
                repl = f'[[{slug}]]' if t==slug else f'[[{slug}|{t}]]'   # ★ 表示は元の表記のまま
                parts[j]=p.replace(t, repl, 1); done=True; break
        if done:
            l=''.join(parts); seen.add(k)
    out.append(l)
open(BOOK,'w',encoding='utf-8').write('\n'.join(out))
print(f"リンク設定完了: {len(seen)} 箇所")
PY
```

### 検証（必須）

```bash
# (1) リンク数が想定の範囲か
grep -o '\[\[[^]]*\]\]' 教科書/<分野>.md | wc -l

# (2) 見出し・コードブロック・付録に混入していないか
grep -n '^#.*\[\[' 教科書/<分野>.md          # 見出しに入っていたらNG
awk '/^## 付録/{f=1} f && /\[\[/{print NR": "$0}' 教科書/<分野>.md   # 付録に入っていたらNG

# (3) 入れ子の壊れがないか（[[[[ や ]]]] が無いこと）
grep -n '\[\[\[\|\]\]\]' 教科書/<分野>.md

# (4) リンク切れ検査（9章の href 突合コマンドを教科書にも適用する）

# (5) ★最重要：aliasリンクが実体ページを指しているか（href実体突合だけでは検出できない）
grep -o '<a[^>]*>[^<]*</a>' 教科書/<分野>.html | grep 'alias' | head -20
# → href が "../用語集/<分野>/xxx" のように用語集フォルダを経由していればOK
# → href が "../xxx"（用語集フォルダを経由しない）だった場合、alias-redirects の空リダイレクトを
#   踏んでいる。ホバープレビューが機能しないので、実行手順のスクリプトが正しく動いていない
```

### ★ (6) F5後マークダウン検証（2026-08-02 に見落として実害が出た）

**リンクを入れるとマークダウンが壊れる。** リンク数やリンク切れをいくら確認しても検出できない。
**ビルド後のHTMLに記法が literal で残っていないか**を必ず確認する。

```bash
# 描画結果に [[ や ** が生テキストで残っていたら壊れている
cd "<Quartzのpublic>"
python3 -c "
import re,html
h=open('教科書/<分野>.html',encoding='utf-8').read()
m=re.search(r'<article.*?</article>',h,re.S)
b=html.unescape(re.sub(r'<[^>]+>','',m.group()))
print(\"literal '[[' :\", b.count('[['))
print(\"literal '**' :\", b.count('**'))
"
# → いずれも 0 でなければ壊れている

# 内容が消えていないかもF5前のバックアップと照合する（実行前に必ずcpを取る）
```

**壊れる3パターンと対処**：

| パターン | 何が起きるか | 対処 |
|---|---|---|
| 表の中の `[[a\|b]]` | `\|` が**セル区切り**と解釈され表が崩壊する | 表の行では `\|` を `\\\|` にエスケープする |
| `**[[語]]**` | 太字の中身がリンクだけ。CJK文脈で `**` がliteral化 | 外側の `**` を外す（リンク自体が十分目立つ） |
| `**前置き[[語]]**` / `**[[語]]後続**` | 太字の**境界にリンクが接する**とliteral化 | リンクを太字の外に出す（`**前置き**[[語]]`） |

> **なぜ起きるか**：CommonMarkの emphasis は「左/右フランキング」条件で判定される。
> 閉じ `**` の直前が `]` や全角の `）` のような**約物**だと右フランキング条件を満たさず、
> 太字として解釈されずに `**` がそのまま表示される。日本語文書で特に起きやすい。

**(4)(5)(6) は必ず行う。** (4)だけでは、alias-redirectsが自動生成する「実体を持つがほぼ空のリダイレクトページ」
を**リンク切れとして検出できない**（実ファイルとしては存在するため）。
2026-08-02の作業で、当初の実装がこの罠にはまり、`[[デジタル署名]]`（alias）のようなリンクが
**クリックすれば正しいページへ着くのにホバーでは何も表示されない**という不具合を作り込んだ。
(4)は「リンク切れではない」ことしか確認できず、「ホバーが機能するか」は別に確認する必要がある。

---

## 2.7 リンク設定はどのモデルでやるか

> **Sonnetで十分。**

- リンク先は**既に確定している**（用語集のファイル群）。設計判断が要らない
- やることは「本文中の語を `[[ ]]` で囲む」**機械的な作業**
- 正誤が**機械的に検証できる**（上記の検証コマンド）
- 手本もある（用語集→教科書のリンク方式）

用語集F2（Sonnetで成功した量産作業）と同じ性質。
**ただしルール（粒度・除外・表記ゆれ）はこの2.6章で確定済みであることが前提。**
ルールが未確定のまま渡すと、リンクだらけの読みにくい教科書になる。

---

## 3. フォルダ構成と命名

```
応用情報技術者試験/
├─ 00_引き継ぎ.md               ← このファイル
├─ 応用情報単語リスト.md         ← 元ネタ（968語・「用語 | 説明」形式）
├─ 教科書/
│   └─ 11_セキュリティ.md         ← 完成（唯一の見本）
└─ 用語集/
    └─ 11_セキュリティ/           ← 完成（140ファイル）
        ├─ 00_混同しやすい語の一覧.md  ← F1の設計成果物 兼 学習用の対比表
        ├─ SQLインジェクション.md
        ├─ ソルト.md
        └─ …
```

### 命名ルール

- **フォルダ名の番号は、IPAの中分類番号に合わせる**（`教科書/11_セキュリティ.md` と `用語集/11_セキュリティ/` が対応する）
  - ただし `01` と `02` は単語リストの区分と中分類が完全には一致していない（両方が中分類2に一部かかる）。実害がないため、この近似を許容する
- **ファイル名は用語そのまま**（`SQLインジェクション.md`）。番号は付けない
  - 理由：`[[SQLインジェクション]]` とリンクしたときにファイル名がそのまま一致するため
- 記号を含む用語は、ファイル名に使えない文字だけ置換する
  - `/` → `-`（例：`JPCERT-CC.md`、`SSL-TLS.md`）
  - 本文の見出し（`# `）には**元の表記をそのまま書く**（`# JPCERT/CC`）

> **⚠️ `/` を含む用語へのリンクは必ずファイル名形式で書く**（2026-08-02 に実際に発生した不具合）
> `[[SSL/TLS]]` と書くと、Quartzは `/` を**パス区切り**と解釈し `ssl/tls` のような
> **存在しない階層**を指してリンクが切れる。表示テキストを保ちたい場合は次の形式を使う：
> ```
> ✕ [[SSL/TLS]]              → ssl/tls を指してリンク切れ
> ○ [[SSL-TLS|SSL/TLS]]      → 正しく解決し、表示は「SSL/TLS」のまま
> ```
> 対象：`SSL-TLS` / `JPCERT-CC` / `ISO-IEC 15408`
- 別名がある語は frontmatter の `aliases` に書く（`` `[[XSS]]` `` でも `` `[[クロスサイトスクリプティング]]` `` でも飛べるようにする。
  ただし2.6章の罠#8のとおり、**教科書からリンクを張るときは alias 表記そのままではなく実ファイル名を主キーにすること**）

---

## 4. 1ファイルのテンプレート（厳守）

```markdown
---
title: SQLインジェクション
aliases:
  - SQLi
分野: 11_セキュリティ
難度: 難          # 易 / 難 のいずれか（→ 6章の基準）
---

# SQLインジェクション

> 入力欄にSQL文の断片を混ぜ込み、データベースに意図しない命令を実行させる攻撃。

## 日常のたとえ

（身近な場面に置き換える。ITの前提知識ゼロでも情景が浮かぶこと）

## なぜそうなるのか

（仕組み・原理。「なぜこの問題が起きるのか」「なぜこの対策が効くのか」）

## 混同しやすい語

| 語 | 決定的な違い |
|---|---|
| [[クロスサイトスクリプティング]] | あちらは情報を「盗む」／こちらは操作を「実行させる」 |

## 試験での問われ方

- 「対策として適切なものはどれか」→ **プレースホルダ**（エスケープは次善策）
- ひっかけ：〜

## 関連

[[WAF]] ／ [[セキュアプログラミング]] ／ [[プレースホルダ]]

教科書：[[11_セキュリティ]] の 2.2 節
```

### 見本ファイル（執筆前に必ず読むこと）

書式の基準はこの4本。**型が違うものを選んである**ので、書く語に近いものを真似る。

| 見本 | 型 | 何の見本か |
|---|---|---|
| `用語集/11_セキュリティ/ディジタル署名.md` | 難 | **鍵の向き**のような取り違えを、対比表と原則で潰す型 |
| `用語集/11_セキュリティ/ソルト.md` | 難 | **なぜ効くのか**を、攻撃側の動機から順に説明する型 |
| `用語集/11_セキュリティ/DMARC.md` | 難 | **必要になった順**で因果を追う型（3語セットの一員） |
| `用語集/11_セキュリティ/NISC.md` | 易 | **たとえ話・混同ペアを省略した型**。制度名・組織名はこれでよい |

### セクションの増減ルール

- **「日常のたとえ」は、無理なら省略してよい**。こじつけたたとえ話は理解を妨げるため、
  自然な比喩が思いつかない語（`CVE`、`NISC` など固有名詞・制度名）では書かない
- **「混同しやすい語」は、実際に紛らわしい相手がいるときだけ**置く。埋めるために無理に作らない
- 逆に、**「一言定義」「関連」は全語で必須**

---

## 5. 執筆ルール

### 文体・レベル

- **想定読者：情報系の大学は出ているが、IT実務経験はなく基本情報レベル未満**
  （教科書 `11_セキュリティ.md` と同じ前提。あの文体をそのまま踏襲する）
- 専門用語を説明に使うときは、**その場で一言添えるか `[[リンク]]` を張る**
- 略語は初出時に**正式名称と、その意味**を書く（例：IDS＝Intrusion **Detection** System＝侵入「検知」）
- 丸囲み数字（①②③）は使わない。`(1)(2)(3)` を使う

### リンク

- 関連語は積極的に `[[用語名]]` で繋ぐ。**まだ存在しないファイルへのリンクでもよい**
  （後から作ったときに自動で繋がる。むしろ「作るべき語」の目印になる）
- **教科書へのリンクを必ず1本入れる**（`[[11_セキュリティ]]` のように）。
  辞書から体系に戻れる導線がこのプロジェクトの要

### やってはいけないこと

- **元の単語リストの一行説明をコピーしただけで済ませない。** 膨らませることが目的
- **教科書の文章をそのまま貼らない。** 辞書として単体で完結する書き方に組み直す
- **不確かな内容を断定で書かない。** 確信が持てない場合はWeb検索で裏を取る
- **`用語集/` を Quartz側（Googleドライブ）で編集しない**（→ 8章）

---

## 6. 難語の選定基準（フェーズ1で使う）

その分野の語を、次の基準で **易 / 難** に振り分ける。
**難と判定した語だけがフェーズ3（Opusレビュー）の対象**になる。

### 「難」に分類する語

- **たとえ話がないと理解できない**（公開鍵暗号、ソルト、ハイブリッド暗号）
- **鍵・向き・順序を取り違えやすい**（ディジタル署名の秘密鍵／公開鍵）
- **似た語と対で問われる**（IDS↔IPS、SPF↔DKIM↔DMARC、FAR↔FRR、27001↔27002）
- **「なぜ必要になったか」の因果が要る**（DMARC、ゼロトラスト、多要素認証）
- **計算式が絡む**（鍵の数 n(n−1)/2、稼働率、ALE＝SLE×ARO）

### 「易」に分類する語

- 制度名・組織名・規格名で、**定義を書けば足りる**（NISC、ISMAP、PCI DSS、CVE）
- 一般語に近く、説明が一意に定まる（情報資産、内部不正）

**目安：難は全体の2〜3割程度**

> **実績（2026-08-02 セキュリティ）**：138語中 **難42語（30%）／易96語**。
> 当初の想定（1〜2割）より多かった。セキュリティは対比で問われる語が特に密集する分野のため、
> 他分野ではこれより下がる可能性が高い。**行数と語数を取り違えない**こと
> （1行に複数語をまとめると集計がずれる。正はファイルの frontmatter `難度:`）。

---

## 7. 進捗表

作業したら**必ずこの表を更新する**。

| # | 分野（フォルダ名） | 語数 | 教科書 | F1 設計 | F2 量産 | F3 レビュー | F5 リンク |
|---|---|---:|:---:|:---:|:---:|:---:|:---:|
| 01 | 基礎理論・アルゴリズム | 74 | | | | | |
| 02 | プログラミング・言語処理 | 14 | | | | | |
| 03 | コンピュータ構成要素 | 52 | | | | | |
| 04 | システム構成要素 | 44 | | | | | |
| 05 | ソフトウェア・OS | 43 | | | | | |
| 06 | ハードウェア | 15 | | | | | |
| 07 | ユーザーインタフェース・情報メディア | 27 | | | | | |
| 09 | データベース | 63 | | | | | |
| 10 | ネットワーク | 74 | | | | | |
| **11** | **セキュリティ** | **140**※ | ✅2026-08-02 | ✅2026-08-02 | ✅2026-08-02 | ✅2026-08-02 | ✅2026-08-02(189本) |
| 12 | 開発技術 | 87 | | | | | |
| 14 | プロジェクトマネジメント | 44 | | | | | |
| 15 | サービスマネジメント・監査 | 58 | | | | | |
| 17 | システム戦略・企画 | 42 | | | | | |
| 19 | 経営戦略・技術戦略 | 48 | | | | | |
| 21 | ビジネスインダストリ・AI利活用 | 40 | | | | | |
| 22 | 企業活動・会計財務 | 57 | | | | | |
| 23 | 法務・標準化 | 46 | | | | | |
| | **合計** | **968** | | | | | |

記入例：`✅2026-08-02` / `作業中(40/138)` / 空欄＝未着手

> **※ セキュリティの +2 について**
> F3レビューでリンク切れ検査を行った結果、単語リストに存在しない語へのリンクが見つかった。
> うち「ファイアウォール」「ワーム」は**既存ページの対比説明が成立するために不可欠**（WAFはFWとの対比が核心、
> トロイの木馬はワームとの対比が核心）だったため、ページを新規作成した（計140ファイル）。
> **`応用情報単語リスト.md` にも追加済み**（966→968語）。以後は上記「単語リストへの追加ルール」に従う。

### 着手順（推奨）

**必ず「教科書 → 用語集」の順で1分野ずつ進める**（→ 1章の大原則）。
下の順序は「どの分野から着手するか」であって、用語集を先行させてよいという意味ではない。

| 順 | 分野 | 理由 | 教科書 |
|---|---|---|:---:|
| 1 | **11 セキュリティ** | 科目B必須・科目Aで最頻出 | ✅完成 |
| 2 | 10 ネットワーク | 科目B選択候補・分量も多い | 未着手 |
| 3 | 09 データベース | 科目B選択候補 | 未着手 |
| 4 | 15 サービスマネジメント・監査 ／ 14 PM ／ 19 経営戦略 | 科目B選択候補（コード不要の文章題） | 未着手 |
| 5 | 残り | | 未着手 |

**現状**：教科書 **1/23**（セキュリティのみ）／用語集 **140語**（セキュリティのみ）。
→ **次にやるのは「10 ネットワークの教科書」**。用語集ではない。

---

## 8. 作業手順

### (1) 対象分野の単語を抽出する

```bash
cd "/Users/yoshidatomohiro/Library/Mobile Documents/iCloud~md~obsidian/Documents/second_brain/002.Notes/002.Privates/勉強/応用情報技術者試験"

# セクション名は 応用情報単語リスト.md の "# ===== 〜 =====" 行と一致させる
awk '/^# ===== テクノロジ系：セキュリティ/{f=1;next} /^# =====/{f=0} f && /\|/{print}' 応用情報単語リスト.md
```

### (2) 執筆する

- 保存先は **iCloud側**（上記パスの `用語集/<分野フォルダ>/`）
- **Quartz側（Googleドライブ）の `content/` は絶対に編集しない**。
  `sync-content.sh` が rsync `--delete` で上書きするため、書いても消える

### (3) 公開サイトに反映する

```bash
cd "/Users/yoshidatomohiro/Library/CloudStorage/GoogleDrive-t.yoshida.0413@gmail.com/マイドライブ/dev/ap-exam-quartz" && ./sync-content.sh
```

これだけで同期・コミット・push・GitHub Actionsによる自動デプロイまで完了する。
公開URL：https://tyoshida0413-ai.github.io/ap-exam-textbook/

### (4) 記録する

- **この引き継ぎドキュメントの進捗表**（7章）を更新する
- 当日のDaylog `## 作業記録 ✏️` に追記する

---

## 9. 完了前チェックリスト

1ファイルごとに確認する。

- [ ] frontmatter に `title` / `分野` / `難度` がある
- [ ] 一言定義（`>` の引用）が1〜2文で書けている
- [ ] 元の単語リストの一行説明の**コピーになっていない**
- [ ] 略語は正式名称と意味が書かれている
- [ ] `[[リンク]]` が最低1本（教科書へのリンクを含む）ある
- [ ] たとえ話は**自然か**（こじつけなら削除する）
- [ ] 1,000〜1,500字に収まっている
- [ ] ファイル名に `/` などの使用不可文字が入っていない

分野を終えたら：

- [ ] **語数の突合**：元の単語リストと作成済みファイルの `title` を `diff` で照合し、差分0を確認した
- [ ] **リンク切れ検査**：下記コマンドで「実体が存在しないリンク」が0件であることを確認した
- [ ] **教科書の節番号が実在するか**確認した（`grep '^#\{2,3\} ' 教科書/*.md` と突合）
- [ ] 進捗表を更新した
- [ ] `sync-content.sh` で公開サイトに反映した
- [ ] Daylogに記録した

> **⚠️ リンク切れ検査は必ずこの方法で行う**
> Quartzは未解決のwikilinkに `class="broken"` を**付けない**ため、
> `grep broken` では検出できない（2026-08-02 にこれで8件見逃した）。
> **出力HTMLのhrefを実ファイルと突き合わせる**のが唯一確実な方法：
>
> ```bash
> cd "<Quartzのpublic>"
> grep -rho 'href="[^"]*"' 用語集/<分野>/*.html \
>   | sed 's/href="//;s/"//' | grep -v '^http' | grep -v '^#' \
>   | grep -v '\.css$\|\.js$\|\.xml$\|\.webp$\|\.png$\|\.ico$' \
>   | sed 's|^\.\./\.\./||' | sort -u \
>   | while read -r h; do h="${h%/}"; [ -z "$h" ] && continue; \
>       [ ! -f "$h.html" ] && [ ! -f "$h/index.html" ] && [ ! -d "$h" ] && echo "切れ: $h"; done
> ```
>
> 検出された語は次のいずれかで処理する：
> (1) 単語リストに**ある** → 後で作られるので放置してよい
> (2) 単語リストに**ない**が対比の要・頻出 → **ページを新規作成し、単語リストにも追加する**（→ 下記ルール）
> (3) 単語リストに**なく**説明も他ページで足りる → **ブラケットを外して平文にする**

### ★ 単語リストへの追加ルール（2026-08-02 決定・許可不要）

作業中に「**元の `応用情報単語リスト.md` に無いが、試験に必要と判断した語**」が出てきたら、
**ユーザーの許可を取らずに、その場で単語リストへ追加してよい**。

- 追加先：`応用情報単語リスト.md` の該当分野セクション。**対比する語の隣**に置く
  （例：ファイアウォールはWAFの直前、ワームはトロイの木馬の直前）
- 形式：`用語 | 説明`（1行1語。説明は1〜2文）
- **禁止事項**：説明文に `[1]` のような角括弧＋数字を書かない（朝のルーティンが参照番号として自動削除する）
- 追加後は必ず検証する：
  ```bash
  # 総語数・分野別語数・重複チェック
  grep -c "^[^#[:space:]].*|" 応用情報単語リスト.md
  awk -F'|' '/^[^#[:space:]].*\|/{gsub(/ +$/,"",$1); print $1}' 応用情報単語リスト.md | sort | uniq -d
  ```
- このファイルは**毎朝のDaylogルーティンが参照**している（ランダム出題の母集団）。
  追加は安全（母集団が増えるだけ）だが、**削除・大幅な書き換えは影響が出る**ので行わない

**追加実績**：2026-08-02 ファイアウォール・ワーム（966→**968語**、セキュリティ138→**140語**）

---

## 10. 決定事項の記録（変更する場合はここを更新する）

| 項目 | 決定 | 決定日 |
|---|---|---|
| **⛔ 制作順序** | **教科書が先、用語集が後。順序は動かさない**（用語集先行はユーザーの明示指示時のみ） | 2026-08-02 |
| **主従関係** | **教科書がメイン。用語集はそれを拡張するために存在する** | 2026-08-02 |
| **教科書のモデル** | **Opus**（手本を作る側であり、章設計とたとえ話の創出が価値の中核のため。→ 2.1） | 2026-08-02 |
| 用語集のモデル | F1設計＝Opus → F2量産＝Sonnet → F3レビュー＝Opus（全件レビューはしない） | 2026-08-02 |
| 想定読者 | 情報系大学卒だがIT実務経験なし・基本情報レベル未満 | 2026-08-02 |
| 用語集の対象 | 単語リストの全語。分野単位で順次 | 2026-08-02 |
| 用語集1語の分量 | 1,000〜1,500字 | 2026-08-02 |
| 用語集の構成 | 分野ごとのサブフォルダ（教科書の中分類番号に合わせる） | 2026-08-02 |
| 単語リストへの追加 | **許可不要**。必要と判断した語はその場で追加してよい（→ 8章） | 2026-08-02 |
| **F5 リンクの粒度** | **段落ごと初出1回＋表の中も対象**（セキュリティで164本）。除外は見出し・コードブロック・付録のみ（→ 2.6） | 2026-08-02 |
| **F5 のモデル** | **Sonnet**（リンク先が確定済みで機械的・検証可能なため。→ 2.7） | 2026-08-02 |
| **F5 のリンクターゲット** | **必ず実ファイル名を主キーにする**（title/aliasの表記のまま使わない）。理由は罠#8（→ 2.6） | 2026-08-02 |
| 表記ゆれ | **同一視する**。ファイルは増やさず `aliases` で吸収（ディジタル署名＝デジタル署名） | 2026-08-02 |
| 着手順 | セキュリティから。次はネットワーク | 2026-08-02 |
| 引き継ぎの範囲 | このファイルは**フォルダ全体**の引き継ぎ（用語集専用ではない） | 2026-08-02 |

---

## 更新履歴

| 日付 | 版 | 内容 |
|---|---|---|
| 2026-08-02 | 1.0.0 | 初版作成（用語集専用として） |
| 2026-08-02 | 2.1.0 | **F5（教科書→用語集のリンク設定）を新設**。2.6章にルール（段落ごと初出＋表も対象・除外場所・表記ゆれ・参考実装・検証手順）、2.7章にモデル判断（Sonnet）を追加。1章の工程を5段階に、進捗表に「教科書」「F5リンク」列を追加 |
| 2026-08-02 | 2.3.0 | **F5後にマークダウン破壊とサイト表示の問題を発見・修正**。(a)表内の`[[a\|b]]`の`\|`がセル区切りと解釈され表が崩壊（12箇所）、(b)`**[[語]]**`等でCJKの`**`がliteral化（計160箇所超）→ 2.6章に**検証(6) F5後マークダウン検証**と壊れる3パターンの表を新設。(c)表のゼブラに使った`var(--highlight)`の実体が**黄色**（`@quartz-themes/core`がconfigを上書き）→中立グレーへ変更、リンクの黄色チップも淡色化。(d)バックリンク141件が目次を圧迫（目次287px）→グラフとバックリンクに上限を設け**目次602pxへ拡大**。罠#9〜#11を追加 |
| 2026-08-02 | 2.2.0 | **セキュリティのF5を実行し完了（189本）。実行中に重大な罠#8を発見・修正**：`[[表記そのまま]]`でリンクすると、aliasの場合に`alias-redirects`が生成する**空のリダイレクトページ**を指し、クリックは通るがホバープレビューが機能しなくなる（href実体突合では検出不可）。titleに`/`を含む語（ISO/IEC 15408等）では罠#4のスラッシュ問題も再発した。対処：**リンクターゲットを実ファイル名（basename）に統一**し、表記は`\|`の後ろに保持する方式に修正。参考実装スクリプトと検証手順(5)をこの方式で全面更新。当初202本→修正後189本 |
| 2026-08-02 | 2.0.0 | **フォルダ全体の引き継ぎに拡張**（`00_引き継ぎ_用語集.md` → `00_引き継ぎ.md` にリネーム）。「セッション開始時の3分ブリーフィング」「0章 フォルダの全体像」「2.1 教科書のモデル選択」「2.5 教科書の作成基準」を新設。大原則「教科書が先、用語集が後」を明文化 |
