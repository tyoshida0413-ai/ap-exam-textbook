# 更新履歴（00_引き継ぎ.md）

> このファイルは `00_引き継ぎ.md` から切り出した履歴。**通常のセッションでは読まない。**
> 「いつ・何が変わったか」を遡って調べるときだけ開く。
> 現在地・ルール・既知の罠・進捗表は [[00_引き継ぎ]] にある。

---

## 更新履歴

| 日付 | 版 | 内容 |
|---|---|---|
| 2026-08-10 | 4.1.0 | **既存4分野の分量不足加筆48本（NW28本・SEC20本）を完了**【Sonnet】。2.3節の発注テンプレートに沿い、NW14本×2バッチ・SEC20本×1バッチをサブエージェントへ発注。<br>**NW14本のうち1バッチがAPI通信エラーで中断**し、14本中9本しか仕様（1,000〜1,500字）を満たしていなかった（残5本は878〜961字で未達のまま停止）。**成果報告を鵜呑みにせず実測したところ発覚**（→ グローバルCLAUDE.mdの「量産・反復作業の検収」原則どおり）。残り5本（ジッタ・回線利用率・輻輳・帯域幅・伝送効率）を別セッションで再発注し仕上げた。<br>検収：`python3 検証.py 10_ネットワーク`（104本・違反0）／`python3 検証.py 11_セキュリティ`（140本・違反0）を実測。難語2本（ジッタ・IDS）を目視レビューし、見本と同水準の質・`難度:`frontmatter保持（罠#22の再発防止）・教科書リンク節番号の維持を確認。<br>難語51本のうち残るのは15分野3本（可監査性・監査証跡・ITガバナンス）の教科書リンク欠落のみ（`検証.py` 全分野実行で違反3本として検出継続中・未着手） |
| 2026-08-08 | 4.0.0 | **制作フローをトークン消費の実測にもとづき全面改定し、引き継ぎ書を分割した。**<br>**(1) 実測**：セッションログ13本（5,109ターン）を集計し、4分野の制作で**約1億6,615万トークン相当**（成果物量の約350倍）を消費していたことが判明。**約58%は「長い会話を毎ターン読み直す費用」**で、実際に生成した本文は12%。教科書の執筆は第50ターン以内・コンテキスト10万以下で完了しており**総消費の1%未満**だった（＝コスト膨張の正体は、教科書を書き終えたあと同じセッションで続けた用語集の量産）。詳細は `00_トークン消費_調査と節約策.md`。<br>**(2) 2章を全面改定**：`/model`切替による工程移行を**廃止**。工程の境目で `/clear`、F2は**サブエージェントに15〜20語ずつ発注**（返させるのはファイル名と `wc -c` の実測値だけ）、という形に変更（→ 2.3節）。**教科書の執筆だけは1本を1セッションで通し、途中で切らない**ことを明記。<br>**(3) 分割**：`00_引き継ぎ.md`（167,975バイト）を **00／01_教科書の作成基準／02_リンク設定F5／03_サイト実装メモ／99_更新履歴** に分割し、**00は85,488バイト（49%減）**に。章参照50箇所をファイル間リンクへ書き換え。分割前後でリンクの消失0件・内容の欠落なしを機械照合で確認。<br>**(4) スクリプト化**：`検証.py`（用語集の機械検査）と `f5_link.py`（F5のリンク付与。`--dry`と自動バックアップ付き。ロジックは従来のまま）を新設。<br>**(5) 初回検査で既存4分野の未修正を検出**：難語51本が仕様1,000字未満、教科書リンク欠落3本。15分野の「用→教」を **✅92/92 → ⚠️89/92** に訂正（→ 7章）。<br>あわせてプロジェクト`CLAUDE.md`に「トークン消費のルール」節を新設し、`index.md`の制作用リンクを更新 |
| 2026-08-06 | 3.18.0 | **15分野の用語集F3（難29語のレビュー）を完了**【Opus】。見本4本（インシデント管理・IT全般統制・RPO・システム監査人）は修正なし。**技術的な欠落を8語で補った**：<br>**(1) ITIL**：ITILの中核である**サービスライフサイクル5段階**（戦略・設計・移行・運用・CSI）への言及が本文に無かった。表で追加し、個別プロセス（インシデント管理・変更管理等）が5段階のどこに属するかも明示。<br>**(2) JIS Q 20000**：**パート構成（-1＝要求事項・認証対象／-2＝実践の手引）**が無かった。教科書が「JIS Q 20000-1における…」という形で出題を扱っているため、この区別が無いと対応できない。<br>**(3) 問題管理**：「根本原因を分析する」と書きながら**分析技法（なぜなぜ分析・特性要因図・パレート分析）が無かった**。加えて**恒久対策の実施はRFC起票→変更管理を通す**という連携が抜けていた（「根本原因が判明したらただちに本番を修正する」は誤り、という頻出論点）。関連リンクに変更管理・RFCを追加。<br>**(4) サービスレベル管理**：**SLAを支えるOLA・UCとの整合性確認**がSLMの活動に含まれる点と、**SIP（サービス改善計画）**が無かった。関連リンクにOLA・UCを追加。<br>**(5) SLA**：**稼働率と年間停止時間の対応表**（99%→3.65日／99.9%→8.8時間）と、SLAの構成要素のうち忘れられがちな**報告・未達時の措置（ペナルティ条項）**が無かった。<br>**(6) 可用性管理**：稼働率の式はあったが**計算例が無かった**。MTTRを20時間→5時間に短縮すると稼働率が96%→99%になる例を追加し、「MTBFを変えなくても直す速さだけで改善できる」ことを数値で示した。SLAとの関係も追記。<br>**(7) リリース及び展開管理**：**展開後の検証とサービス受入基準**への言及が無かった（「リリースは本番反映で完了ではない」）。<br>**(8) テストデータ法**：**テストデータが本番データベースに残る危険**と、その対処（テスト環境で実施する／識別して確実に削除する）が無かった。ITF法との運用上の違いも明示。<br>**リンクの穴も修復**：`[[修正]]`（ワークアラウンド）が実ファイル不在で**リンク切れ**だったため `[[不適合及び是正処置\|修正]]` に修正（罠#8）。未作成語への参照3件（サービスの設計及び移行×2・運用の資源管理・統制活動）を実在語へ置き換え。<br>検証：**15分野の未解決リンクは `グリーンIT` の1件のみ**（F1設計どおり他分野の既存語なので意図的）。難29語すべて1,000字以上を維持。ビルドで生`**` 0・生`[[` 0・教科書リンク413本すべて到達。本番反映を実測。次はF5【Sonnet】 |
| 2026-08-06 | 3.17.0 | **15分野の用語集F2（量産・残り87語）を完了**【Sonnet】。全92語（見本5語＋F2の87語）が出揃った。<br>**1回目の執筆では、易語60語が仕様の1,000〜1,500字を大きく下回っていた**（300〜990字。→ 罠#22の再発）。全60語に追加の段落・具体例・表の行を加えて拡充し、**全92語が本文1,000字以上を満たすことを実測確認**（frontmatterを除いた本文の文字数で判定。バイト数では日本語とASCIIの混在比率が語ごとに違うため判定に使わない）。<br>F1設計文書の難30語リストに誤りを発見：見本として「易」で執筆した[[サービスカタログ]]が難リストに残っていた（記載漏れ）。設計文書側を訂正し、難29語／易63語に修正。ファイル側の`難度:`frontmatterとの突合で不一致0件を確認。<br>ビルド検証で罠#9（表内`\|`未エスケープ）を新規3箇所（標準変更・予防処置・構成品目）で発見・修正。さらに**罠#19の新規発生を22ファイルで発見・修正**（型は罠#33の(a)〜(d)の応用で全件対応）。この過程で**プロジェクト全体を横断チェックしたところ、2026-08-02のF5作業に由来する未エスケープが11_セキュリティに2件残っていた**ことが判明し修復（→ 罠#36を新設。個別対応だけでなく全文横断チェックを定例化する）。<br>最終確認：全用語集・教科書ページで生`**` 0・生`[[` 0、教科書リンク413本すべて到達。本番デプロイし、内部監査ページの生`**` 0を実測 |
| 2026-08-06 | 3.16.0 | **⛔ 用語集→教科書のリンクが322ファイル全部404だったことが発覚し、修復した**【Opus】。15分野のF1で見本を書き、ビルド後のhrefを確認したところ `教科書：[[15_サービスマネジメント・監査]]` が**用語集フォルダ**を指していた。既存3分野を調べると `[[09_データベース]]` は**存在しないルート直下**を指しており、本番で **`/09_データベース`・`/10_ネットワーク`・`/11_セキュリティ` がいずれも404**（実測）。<br>**原因**：Quartzのwikilinkは `教科書/` 配下まで解決せず、教科書側に `aliases` も無かった。**にもかかわらず「用→教 ✅76/76・104/104・140/140」と記録していた**のは、検証コマンドが `grep -l "教科書：\[\["` ＝**文字列の有無しか見ていなかった**ため（→ **罠#35を新設**。罠#3・#22と同じ「書いてあるか＝効いているか」の取り違え、3度目）。<br>**修復**：ユーザー承認を得て、322ファイルの末尾1行を `教科書：[[教科書/<分野>\|<分野>]]` へ一括置換。11_セキュリティの `00_混同しやすい語の一覧` にはリンク自体が無かったので追加。**ビルド後HTMLのhrefを実体ファイルと突合し、329本すべて到達・到達不能0**を確認。本番でも4分野から1本ずつ200を実測。<br>7章の「用→教」列の注記を全面改訂し、**正しい書き方と、href実体突合のPythonスクリプト**を記載した。<br>**あわせて、同じ検証漏れで見逃していた罠#19の破損を11_セキュリティの19ファイル22箇所で発見・修復**（`**〜（〜）**を` `**「〜」**です` など。SOCの `**〜（[[CSIRT]]）**です` は罠#21も併発）。全用語集・教科書ページで**本文の生`**` 0**を確認 |
| 2026-08-06 | 3.15.0 | **15分野の用語集F1（設計）を完了**【Opus】。`用語集/15_サービスマネジメント・監査/00_混同しやすい語の一覧.md` を作成（最重要の混同ペア22組、セットで理解すべき語のかたまり11群、**難30語／易62語**の判定、F2の進め方、この分野特有の注意）。<br>**見本5本を執筆**（型が違うものを選んである）：**インシデント管理**（対になる語との切り分けを軸に組む型）／**IT全般統制**（どちらに分類されるかの判別軸を読者に渡す型）／**RPO**（図と時間軸で取り違えを潰す型）／**システム監査人**（やってはいけないことの列挙から原則を導く型）／**サービスカタログ**（易の最小構成）。<br>**F1の突合で、単語リストに無い34語を追加**（58→92語、総数1011→1045語。→ 7章※4）。データベース13語・ネットワーク30語に続き、**教科書を出題範囲基準で書くと単語リストの不足が自動的に洗い出される**ことを3度目に確認。<br>**他分野に既にある語（MTBF・稼働率・RASIS・フォールトトレラント等13語）はこの分野で作らない**と決め、設計文書に明記。<br>検証：全ファイル3,800〜5,300バイト（下限3,000超）／難度はF1設計と一致／参照した節（1.4・5.1・6.2・10.2・11.4）が実在／ビルド後の本文の生`**` 0・生`[[` 0。<br>途中で罠#9（表内の `\|` 未エスケープでセルが崩壊）と罠#19を計4箇所踏み、修正した。次はF2【Sonnet】 |
| 2026-08-05 | 3.14.0 | **教科書4本目『15_サービスマネジメント・監査』を作成（63,490字・0〜12章＋付録A/B/C）**【Opus】。**IPA中分類15（サービスマネジメント）と16（システム監査）を1ファイルに統合**（「作った後のITを、回す側と確かめる側」で地続きのため）。<br>**2.5章の正しい手順を初めて最初から守った**：出題範囲詳細から知識項目例を抜き出し、**節への割付表を先に作って空欄なしを確認してから執筆**。結果、ネットワーク・セキュリティで発生した「後から出題範囲の抜けを補完する」作業がゼロになった。執筆後の機械照合でも未出現は「秘密の保持」1件のみ（`守秘義務` と書いていたため `守秘義務（秘密の保持）` に改めて解消）。<br>付録Bは**科目A四択12問＋科目B記述4問**（仕様の10問＋記述を上回る）。科目Bは実際の出題形式に合わせ、**企業の運用状況を箇条書きで示して不備を指摘させる型**にした。<br>**ローカルビルド検証で罠#19（日本語文中の太字が壊れる）を19箇所検出・修正**。`**【まず日常の話】**` のような**行末で閉じる形は壊れない**ため、`grep '[）」』】]\*\*'` では150件以上が誤検出になり本当の19件が埋もれることが分かった（→ **罠#33を新設**。壊れる条件と4つの修正の型を記載）。修正後に再ビルドし**生`**` 0・生`[[` 0**を確認。本番でも新規本文にしか出ない語（`フォロー・ザ・サン`／`並行シミュレーション法`）で反映を実測。<br>**あわせて09データベース教科書のF5事故を1件修復**：`属性（アトリビュート）` が `属性（アトリ[[ビュー]]ト）` になっていた（用語集の`ビュー`がカタカナ語の内部にマッチ）。罠#20は英字略語の話だったが**カタカナでも起きる**ため → **罠#34を新設**（点検コマンド付き）。<br>**判断待ちだった `CLAUDE.md` の公開も解消**：ユーザー判断で除外を選択し、`sync-content.sh` の rsync に `--exclude='CLAUDE.md'` を追加＋リポジトリから削除。本番で **404** を実測確認（公開リポジトリのため履歴には残る）。<br>次は15の用語集F1、または5分野目の教科書 |
| 2026-08-05 | 3.13.0 | **iPadの表示崩れの原因を実機で確定し、自己修復を常設した**【Opus】。前日の「サイト側に不具合なし・一時的な事象」という結論は**誤り**で、崩れは再発していた。`表示診断.md` を崩れているタブで開いてもらった結果、**メインCSS `index-*.css` のルール数が `0 件`**（`<link>` は最新の正しいファイル名を指し、サーバは同時刻に HTTP 200・38,488バイトを返していた）と判明。404でも `@layer` 非対応でも画面幅でもなく、**その1ファイルの取得だけが端末側で失敗し、失敗結果がキャッシュされてリロードでも直らない**事象と確定（→罠#31を全面改訂）。<br>対処として `quartz/components/Head.tsx` に**自己修復スクリプト cssGuard** を追加。`custom.scss` の目印 `--main-css-loaded` が無ければ `?cssguard=<時刻>` を付けて最大3回読み直し、キャッシュされた失敗を回避する。結果は `localStorage.cssGuardLog` に記録し、`表示診断.md` の「自己修復の履歴」に表示（**再発の証拠がデプロイをまたいで残る**）。SPA遷移で再実行しないよう `data-persist` を付与。<br>製作中に罠#32（判定にメディアクエリ内の変数 `--left-col-width` を流用してスマホ幅で誤発火）を踏み、専用の目印を作る方式に修正。<br>CDP実測5パターン：正常系＝発火なし／初回だけ失敗＝`retry1 → recovered`（grid列が `280px 796px 280px` に復旧）／常に失敗＝`retry1→2→3→failed` で停止／スマホ幅390px＝発火なし／復旧後のSPA遷移でCSS維持。本番反映も確認済み（新CSS `index-20428424.css` に目印あり・旧CSSは404） |
| 2026-08-04 | 3.12.0 | **暗記カードを教科書・用語集と同じフォルダ表示にした**【Opus】。従来は `暗記カード.md` 1枚だけで、左サイドバーでは他の資料と同列の1行にしか見えず、分野を選ぶには一度ページを開く必要があった。`暗記カード/` フォルダ＋`index.md`・`09_データベース.md`・`10_ネットワーク.md`・`11_セキュリティ.md`・`全部.md` に分割し、**サイドバーから分野を1クリックで開けるようにした**。各カテゴリページは静的アセット（`static/flashcards/`）を **iframeで埋め込み**、そのページの中でカードが動く（下に「⤢ 全画面で開く」も併置）。<br>**カード側テンプレート（`quartz/tools/flashcards-template.html`）に埋め込みモードを追加**：`window.self !== window.top` で判定して `html.embed` を付与し、(1) 自前のヘッダー（カテゴリ選択・テーマ切替）を非表示、(2) 背景を透明にして親の地色に載せる、(3) 同一オリジンなので `window.frameElement.style.height` を **ResizeObserver で中身の高さに追従**させ余白・内側スクロールを無くす、(4) テーマは親（Quartz）に追従。<br>**罠#30を新設**：Quartz v5 のテーマトグルが `:root` に付ける属性は **`saved-theme`** であって `data-theme` ではない（`custom.scss` 冒頭のコメントは `data-theme` と書いてあるが、v5では効いていない）。最初 `data-theme` を監視して実装したところ、ヘッドレスChromeでの実測でトグルに追従しないことが判明し `saved-theme` に修正。<br>iframeのsrcは `.././static/flashcards/#security` の相対＋ハッシュ（クエリ`?`はQuartzのリンク変換で落ちる＝既知の罠）。ローカルと本番の両方で、サイドバー階層・iframe解決先URL・カード枚数（76／104／140）・答えの表示・SPA遷移・テーマ切替の追従・スマホ幅の横スクロール0 をヘッドレスChrome（CDP）で実測して確認しデプロイ済み |
| 2026-08-04 | 3.11.0 | **付箋（しおり）機能と端末間同期を実装・デプロイ**【Opus】。教科書1ページが13万字級で、中断すると最初から読み直しになる問題への対応。右下の🔖で読んでいる位置に付箋を貼り、≡の一覧から**どのページからでも別ページの続きへ直行**できる。付箋のあるページを開くと「前回の続き（見出し名）から読む」バナーが本文の真上に出る。位置は**スクロール量ではなく直近の見出しid**で保持（本文を加筆してもズレない）。<br>**iPadとMacでの共有**についてユーザーから質問があり、WebSearchで**iCloudはlocalStorageを同期しない**（同期対象はブックマーク・リーディングリスト・タブグループ・設定のみ）ことを確認。**Cloudflare Worker + KV** による同期を追加した（`sync-worker/`。**既存のCloudflare運用に触れないよう、専用Workerを新規に1つ作る方式とし、デプロイはユーザーの手動運用のまま**。未設定なら従来どおり端末内で完結する）。合言葉はサーバーに保存せずSHA-256のハッシュをKVキーに使う。<br>**CDP実測で計36項目すべて合格**（機能18＋同期18）。本番URLでも動作確認済み。<br>**実装中に4つの罠を踏み、#26〜#29として新設**：(1) スクロールしてから要素を消すと**74pxずれる**（順番の問題）。(2) **HTTPヘッダーに日本語を入れると`fetch`が例外**を投げ同期が丸ごと動かない（**ASCIIだけのテストでは絶対に再現しない**）。(3) **`location.pathname`の表記揺れ**で同じページが別ページ扱いになり端末間で付箋が二重化する。(4) **削除をレコード削除で実装すると他端末から復活する**（墓標が要る）。<br>いずれも**「動いているように見える」状態をすり抜けるもので、実測して初めて検出できた**（特に(2)(3)(4)は同期を実際に2端末ぶん往復させないと出ない） |
| 2026-08-04 | 3.10.0 | **データベースのF5（教科書→用語集のリンク設定）を完了**【Sonnet】。参考実装スクリプト（2.6章）に罠#20対策（英数字の単語境界チェック）を組み込んで実行し、**233本**のリンクを設定（セキュリティ189本／ネットワーク404本）。<br>実行直後のビルド検証で**3種類の破損**を発見・修正：**(1)** 罠#21パターン1（`**[[語]]**`）が**56箇所** → 外側の`**`を除去。**(2)** 表内の`[[語|表示]]`で`|`がセル区切りと解釈され**9箇所**で表が崩壊（罠#9の再発）→ `\|`にエスケープ。**(3)** **新種の破損を発見**：インラインコード（バッククォート1つ）内にリンクが挿入され、`` `[[HAVING句\|HAVING]]` ``のようにコードとして表示されリンクが機能しない箇所が**5箇所**（`HAVING`×4・`NULL`×1）→ リンクを外して元の文字列に復元。**罠#23として新設**（F5スクリプムは```コードブロックは除外していたが単一バッククォートは除外していなかった）。<br>さらに罠#21パターン2/3（太字境界にリンクが接する）が2箇所残っており個別修正。最終的に生`**`0・生`[[`0・リンク切れ0・aliasリダイレクト0を確認。<br>**リンク網羅性チェックで想定外の発見が2件**：**(a)** 5語（BIツール・カーソル・ストアドプロシージャ・ウォームスタート・シャーディング）が**教科書本文に一度も出現していなかった**（罠#24として新設。F1の単語リスト突合では気づけなかった逆方向の抜け）。**(b)** 6語（SELECT文・GROUP BY句・EXISTS述語・デッドロックの検知・参照整合性制約・DDL・DML・DCL）は本文に**表記違いで出現**しており機械置換では拾えず、手動でリンクを追加（罠#25として新設）。(a)の5語は本文への追記をせず、**ユーザーへの報告と対応方針の判断待ち**とした。デプロイ済み。**09データベース分野が教科書・用語集ともに完成**。次は4番目の分野の教科書執筆【Opus】 |
| 2026-08-04 | 3.9.0 | **データベースの用語集F3（難23語のレビュー）を完了**【Opus】。見本4本（第3正規形・HAVING句・ロールフォワード・インデックス）は修正なし。**残る19本を全面的に加筆・修正**した。<br>**(1) 想定外の発見＝仕様の量的未達**：F2産の19本が**300〜450字**しかなく、仕様（1語1,000〜1,500字）の約3分の1だった。見本にある「日常のたとえ」「手を動かす実例」が省かれていた。**語数照合・リンク切れ検査・太字検査はすべてこれをすり抜ける**ため、F2完了時には気づけなかった（→ **罠#22を新設**）。<br>**(2) 難度frontmatterの不整合**：F1で「難」とした9語（正規化・第2正規形・部分関数従属・推移的関数従属・関係代数・左外部結合・排他制御・ロールバック・2相コミット）が `難度: 易` に書き換えられていた。設計を正として全23語を`難`に戻し、一覧側の注記（「ファイル側を信じる」）も**設計側を正**に改めた。<br>**(3) 技術的な問題**：関係代数が「8演算とSQLの対応を問う出題」と書きながら**5演算しか載せておらず「商」が欠落**、「積（積集合）」と「直積」の表記が衝突していた。<br>**(4) 試験に出るのに書かれていなかった欠落**：外部キーの**NULL許容**（主キーとの決定的差）と参照動作（CASCADE・RESTRICT・SET NULL）／NULLの**3値論理**・NOT INの罠・一意性制約はNULLを複数許す／左外部結合の**COUNT(\*)が社員0人の部署を1と数える罠**とWHERE句で内部結合に戻る罠／排他制御の**デッドロックへの言及が本文・関連ともゼロ**・両立性の表／射影の**重複行除去**（SQLとの差）／関数従属の**完全関数従属・決定項・従属項**の定義（他ファイルがこの語を使っているのに未定義）／ボイス・コッド正規形の**第3正規形との差が出る具体例**／ACID特性の**一貫性を守る仕組み**（制約）とIsolationの訳語統一／第2正規形の**単一列主キーなら自動的に第2正規形**という判定と分解の実例／副問合せのIN・ANY・ALL・導出表／2相コミットのセキュア状態とブロッキング。<br>ローカルビルドで**生`**`0件・生`[[`0件・リンク切れ0件・aliasリダイレクト0件**を確認しデプロイ。次はF5【Sonnet】 |
| 2026-08-03 | 3.8.0 | **データベースの用語集F2（残り71語の量産）を完了**【Sonnet】。単語リスト76語全てのファイルを作成し、title照合で76/76・差分0を確認。ローカルビルド検証で罠#19・罠#21を計5箇所発見・修正（3層スキーマ・カーソル・スタースキーマ・ACID特性・集約関数）。リンク切れも1件発見（`[[第2正規化]]`が実ファイル名と不一致）し、`[[第2正規形|第2正規化]]`に修正して解消。最終的に生`**`0・リンク切れ0を確認しデプロイ。次はF3（難23語のレビュー）【Opus】 |
| 2026-08-03 | 3.7.0 | **データベースの用語集F1（設計）を完了**【Opus】。`用語集/09_データベース/00_混同しやすい語の一覧.md` を作成（最重要の混同ペア18組、セットで覚える語のかたまり9群、難23語／易53語の判定、F2の進め方、ネットワークで踏んだ罠#19・#21への注意書き）。見本5本を執筆：**第3正規形（手順を実例で見せる型）／HAVING句（紛らわしい相手との切り分け型）／ロールフォワード（対になる語と使い分けの判断基準を示す型）／インデックス（トレードオフを正面から扱う型）／データマート（易の最小構成）**。**F1の突合で、単語リストに無い13語を追加**（63→76語、総数998→1011語）。教科書を出題範囲基準で書いたことで、**単語リスト側の不足が自動的に洗い出される**という副次効果を確認。ローカルビルドで罠#19を3箇所検出・修正し、最終的に生`**`0を確認してデプロイ |
| 2026-08-03 | 3.6.0 | **教科書3本目『09_データベース』を作成（約37,400字・0〜6章＋付録A/B/C）**。**IPA出題範囲（中分類9・小分類1〜5）の知識項目例だけを起点に章立てを構成し、単語リストは一度も開かなかった**（2.5章冒頭の手順を初めて最初から適用）。構成：0章 DBMSと3層スキーマ／1章 関係データベースの構造と関係代数／2章 設計と正規化（第1〜第3正規形を実例で分解）／3章 SQL（DDL・DML・DCL、GROUP BYとHAVING、結合、副問合せ、ビュー）／4章 トランザクション（ACID・排他制御・デッドロック・分離レベル・障害回復・性能向上・アクセス制御）／5章 応用（DWH・データマイニング・分散DB・リポジトリ・ビッグデータとNoSQL）／6章 科目B対策。付録Bは四択12問＋記述3問。**出題範囲の知識項目例21語すべての本文収録を機械照合で確認**。ローカルビルドで罠#19を12箇所検出・修正し、最終的に生`**`0・生`[[`0を確認してデプロイ。分量は目安（20,000〜30,000字）を超過したが、SQLの実例と正規化の分解過程を削らない判断（→ 2.5章の方針どおり） |
| 2026-08-03 | 3.5.0 | **ネットワークのF5（教科書→用語集のリンク設定）を完了**【Sonnet】。104語・**404本**のリンクを設定（セキュリティは140語189本）。参考実装スクリプト（2.6章）をそのまま使い、**罠#20対策（英数字の単語境界チェック）を追加**して実行し、罠#20の再発は0件だった。しかし新たに**罠#9の亜種を約95件**踏んだ：F5が挿入した`[[語]]`が既存の太字（教科書執筆時点のもの）と密着し、`**[[単一障害点]]**`型（約90件）と`**典型的な[[語]]**です`型・`そこで**[[語]]もセット**`型（5件）の両方が壊れた。壊れる条件がCommonMarkの理論だけでは説明できず、同じパターンでも壊れる箇所と壊れない箇所が混在したため、正規表現一括判定は諦め、**ローカルビルド→生`**`のgrep→該当箇所の太字を外す**というサイクルを2周して収束させた（→ 罠#21）。あわせて表内の`[[a|b]]`エスケープ漏れ8件（罠#9本来のパターン）も修復。最終確認：単語リスト104語すべてが本文からリンク済み（未リンク5件は教科書側の表現を自然な形で調整して解消：telnetとSSH・全二重と半二重・プロキシサーバ・ホスト数の計算）。ローカルビルドで生`**`0・生`[[`0・リンク切れ0・alias-redirect異常0を確認しデプロイ。**ネットワーク分野が教科書・用語集ともに完成** |
| 2026-08-03 | 3.4.0 | **ネットワークの用語集F3（レビュー）を完了**【Opus】。難語をfrontmatterで再集計したところ**33語**（一覧の30語と不一致）だったため、一覧を33語構成に修正（IPアドレス・VRRP・広域イーサネットを難へ移動）。33語を精読し、**技術的な誤り2件・試験に出る事項の欠落8件・表現の問題1件**を修正。誤り：(a)[[STP]]がTTLを `[[MTU\|TTL]]` とリンクしていた（TTLとMTUは別概念で、引きたい人が違うページへ飛ぶ）、(b)[[MTU]]のページにTTLの説明が同居し1ファイル1単語の原則に反していた。欠落：CIDRの経路集約／UDPの1対多の同報通信／RIPの収束の遅さ／OSPFのエリア／BGPのパスベクトル型／セッション管理のSecure・HttpOnly／HTTP/3の接続確立の速さとコネクション移行／TCPのフロー制御と輻輳制御の区別。**F2（Sonnet）の記述に技術的誤りはほぼ無く、欠落が難語に集中**という傾向を一覧の4章に記録。次はF5【Sonnet】 |
| 2026-08-03 | 3.3.0 | **ネットワークの用語集F2（残り99語の量産）を完了**【Sonnet】。単語リスト104語全てのファイルを作成し、title照合で104/104・差分0を確認。ローカルビルド検証で日本語に挟まれた太字の破損（罠#19）を**32箇所**発見・修正し、さらに新種の破損を2件発見：**(a) 太字がwikilinkを密着で囲む**（`**[[単一障害点]]**` が壊れる。罠#9と同種だが今回はF2執筆時点の混入）、**(b) 太字の先頭が半角スラッシュ**（`**/24**` が壊れる。空白を挟めば発生しない）。(a)は太字を外し、(b)はコードスパン`` `/24` ``に置換して解決。再ビルドで104ファイル全ての生`**`が0件になったことを確認し、push・デプロイ済み。**罠#19に(a)(b)の2パターンを追記**。次はF3（難30語のレビュー）【Opus】 |
| 2026-08-03 | 3.2.0 | **ネットワークの用語集F1（設計）を完了**【Opus】。`用語集/10_ネットワーク/00_混同しやすい語の一覧.md` を作成（最重要の混同ペア20組、セットで覚える語のかたまり9群、難30語／易74語の判定、F2の進め方、`/`を含む語のファイル名規則）。見本5本を執筆：**サブネットマスク（計算型）／CSMA/CD（対比型）／3ウェイハンドシェイク（因果型）／ウィンドウ制御（紛らわしい2つの制御を切り分ける型）／テザリング（易の最小構成）**。ローカルビルドで壊れた太字0件を確認。**次はF2（Sonnetで残り99語）** |
| 2026-08-03 | 3.1.0 | **11_セキュリティを出題範囲（中分類11・小分類1〜5）と突合し、抜け4項目を補完**（教科書1.1.0）。追加：2.6 攻撃者の種類と動機／5.5 情報資産の調査・分類とリスクの種類（純粋リスクと投機的リスク）／5.6 情報セキュリティ継続とBCPの関係。付録Aに想起項目、付録Bに3問を追加。**既存の節番号は用語集140ファイルが参照しているため一切変更していない**（新設は各章の末尾に追加）。あわせて**F5が単語内部を置換して壊していた4件を修復**（GPKI・PDCA・HMAC・DDoS攻撃 → 罠#20）。小分類3・4・5は抜けなしを確認。表記ゆれで未収録に見えた項目（ディジタル証明書＝デジタル証明書、脆弱性検査＝脆弱性診断、侵入検知＝IDS、SSL/TLS＝TLS、セキュアプログラミング＝8.3節）はすべて収録済みだった |
| 2026-08-03 | 3.0.0 | **プロジェクトの主目的と、資料の主従関係を厳密化**（ユーザー指示）。目的は「**試験範囲を網羅して合格すること**」であり、**教科書がその主目的そのもの**、用語集と単語リストはその補助であると冒頭に明記。**教科書の工程では `応用情報単語リスト.md` を一切参照しない**（骨格にも完了判定にも使わない）というルールを、冒頭・「絶対に守る6つのルール」・2.5章冒頭・1章の工程図・8章・9章・10章の決定事項に反映。9章に**教科書専用の完了前チェック（9-0）を新設**し、判定を「出題範囲詳細の知識項目例を1つ残らず説明したか」だけに限定。進捗表の「語数」列は用語集の指標であって教科書の完成度とは無関係である旨を明記 |
| 2026-08-03 | 2.9.0 | **🛑 教科書の作り方の欠陥を修正。** ネットワーク教科書の初版は**単語リストを骨格にして組み立てており、IPA出題範囲にある項目が抜けていた**（ユーザー指摘で発覚）。`応用情報技術者試験_出題範囲詳細.md` の中分類10・小分類1〜5と突合し、抜けていた項目を教科書1.1.0で補完（LAN/WAN、イントラネット・エクストラネット、伝送方式と回線、多重化、誤り制御・ARQ、HDLC、回線接続装置、単一障害点・STP・リンクアグリゲーション・VRRP、HTTP/2・HTTP/3、CORBA、通信サービス、運用管理、ロードバランサ、IEEE 802.11規格・WPA3、LTE、センサーネットワーク）。**2.5章の冒頭に「章立ての起点はIPA出題範囲であって単語リストではない」という手順を新設**し、検証も2本立て（単語リスト照合＋出題範囲の項目突合）に変更。単語リストへ30語を追加（968→998語、ネットワーク74→104語） |
| 2026-08-03 | 2.8.0 | **教科書2本目『10_ネットワーク』を作成（約46,500字・0〜8章＋付録A/B/C）**。単語リストのネットワーク**74語すべてを本文に配置**し、機械照合で未出現0を確認。**全ての節に番号を振り**（罠#5の回避）、用語集からは「N.M 節」で参照できる。F5前なのでwikilinkは0本（意図どおり）。**初回デプロイ後の本番HTML実測で、日本語に挟まれた太字14箇所が生の `**` のまま表示されていることを発見・修正して再デプロイ**（→ 罠#19。閉じる `**` の直前が全角括弧だと太字にならない。**mdの目視では検出できないため、出力HTMLで `grep -o '\*\*'` が0件であることを必ず確認する**）。**2.5章に分量超過の実績と、超過時は付録から削り因果説明は削らないという方針を追記**。進捗表・着手順・決定事項を更新 |
| 2026-08-03 | 2.7.0 | **TODO-2を自作の用語サイドパネルとして実装し直し、CDP実測で全23項目合格**（ローカルのみ・未push）。stacked-pagesは無効化。設計と実装結果を**2.9章**に新設（2.8章は失敗の記録として保持）。実装中に踏んだ罠を#15〜#18として追加：(15)`componentResources.css` に渡したCSSは `@layer quartz-base` 内かつ base.scss より前に置かれレイアウト指定が効かない→custom.scssから`@use`する、(16)SPAルータは`defaultPrevented`を見ないため`preventDefault`だけでは遷移を止められない→`stopPropagation`が必要、(17)`.center`は自動配置のグリッドアイテムでサイドバーを`display:none`にすると本文が48pxに潰れる→`visibility:hidden`＋幅0にする、(18)`scroll-behavior:smooth`のためCDPでのスクロール計測が誤検知する |
| 2026-08-03 | 2.6.1 | **🛑 訂正：TODO-2（Stacked Pages）は「完了」ではなかった。** push・デプロイ後にユーザーが実際のブラウザで確認したところ見た目が破綻しており、調査の結果、(1) 有効なドキュメント（`docs/plugins/StackedPages.md`）の説明「元ページを残して並べて表示」自体が実装（通常のページ遷移＋タブ帯のみ）と一致しておらず、**本来の要望を満たしていなかった**、(2) 2ページ遷移のみのCDP検証では見つからなかった「Home→用語集→11_セキュリティ→機密性」の4段階遷移で、タブ積み上げによる紛らわしい表示（サイト名の二重表示）が発生することが判明。「検証済み」としていた記述を訂正し、2.8章に訂正セクションを追加。**対応（無効化に戻す等）はユーザー指示待ちで、次の担当は勝手に実装を進めないこと** |
| 2026-08-03 | 2.6.0 | **TODO-1（本文幅）・TODO-2（Stacked Pages）を実装し、CDP実測で検証**（Quartzリポジトリのローカル変更のみ・push未実施）。2026-08-02の原因分析が誤りだったと判明：真因は `@quartz-themes/core` が注入するid込み詳細度のunlayered CSS。`custom.scss` セクション1を全面書き換え（`div#quartz-root.page div#quartz-body` を詳細度で上書き）、`quartz.config.yaml` で `stacked-pages` を有効化。有効化直後は左サイドバーとテキストが重なるバグを発見し、`custom.scss` セクション4で修正。2.8章を全面更新、罠#13・#14を追加 |
| 2026-08-02 | 1.0.0 | 初版作成（用語集専用として） |
| 2026-08-02 | 2.1.0 | **F5（教科書→用語集のリンク設定）を新設**。2.6章にルール（段落ごと初出＋表も対象・除外場所・表記ゆれ・参考実装・検証手順）、2.7章にモデル判断（Sonnet）を追加。1章の工程を5段階に、進捗表に「教科書」「F5リンク」列を追加 |
| 2026-08-02 | 2.5.0 | **未着手TODO 2件を2.8章として記録**（実装はしていない）。(1)高解像度モニターで本文が広がらない問題（3440pxでも本文930px＝27%。原因は`#quartz-body`のグリッドが1500pxで頭打ち）＝方針「画面幅可変・上限1400px」で決定済み。(2)用語クリックでサイドパネルに表示＝**Quartz標準の`stacked-pages`プラグインで実現可能**と判明（現在`enabled:false`・本体未インストール）。**両方ともユーザーから「翌日こちらから指示する」との指示があり着手しない**。3分ブリーフィングにもTODO表を追加 |
| 2026-08-02 | 2.4.0 | **表の配色を作り直した**。2.3.0で黄色を消す際に薄くしすぎ、ダークで**ヘッダrgb(40,40,40)と偶数行rgb(35,35,35)の差がわずか5**となり見分けがつかなくなっていた（ユーザー指摘）。加えて**行の横罫線が0pxで消えていた**。→ `--tbl-head/zebra/line/line-strong` をライト・ダーク別に定義し、**横罫線で行を区切る**方式へ変更。実測でヘッダと偶数行の差をダーク22／ライト12に確保。罠#12を追加 |
| 2026-08-02 | 2.3.0 | **F5後にマークダウン破壊とサイト表示の問題を発見・修正**。(a)表内の`[[a\|b]]`の`\|`がセル区切りと解釈され表が崩壊（12箇所）、(b)`**[[語]]**`等でCJKの`**`がliteral化（計160箇所超）→ 2.6章に**検証(6) F5後マークダウン検証**と壊れる3パターンの表を新設。(c)表のゼブラに使った`var(--highlight)`の実体が**黄色**（`@quartz-themes/core`がconfigを上書き）→中立グレーへ変更、リンクの黄色チップも淡色化。(d)バックリンク141件が目次を圧迫（目次287px）→グラフとバックリンクに上限を設け**目次602pxへ拡大**。罠#9〜#11を追加 |
| 2026-08-02 | 2.2.0 | **セキュリティのF5を実行し完了（189本）。実行中に重大な罠#8を発見・修正**：`[[表記そのまま]]`でリンクすると、aliasの場合に`alias-redirects`が生成する**空のリダイレクトページ**を指し、クリックは通るがホバープレビューが機能しなくなる（href実体突合では検出不可）。titleに`/`を含む語（ISO/IEC 15408等）では罠#4のスラッシュ問題も再発した。対処：**リンクターゲットを実ファイル名（basename）に統一**し、表記は`\|`の後ろに保持する方式に修正。参考実装スクリプトと検証手順(5)をこの方式で全面更新。当初202本→修正後189本 |
| 2026-08-02 | 2.0.0 | **フォルダ全体の引き継ぎに拡張**（`00_引き継ぎ_用語集.md` → `00_引き継ぎ.md` にリネーム）。「セッション開始時の3分ブリーフィング」「0章 フォルダの全体像」「2.1 教科書のモデル選択」「2.5 教科書の作成基準」を新設。大原則「教科書が先、用語集が後」を明文化 |
